//! Phase 26 OBS-01: up-front 6-channel registration (Wave 3 plan).
//!
//! STUB — implemented by a later wave. Kept as a doc-only module so the
//! `observability` barrel in `mod.rs` compiles. The Wave 3 plan replaces
//! this file with the channel-registration helper that hands the schema
//! descriptors from `schema_registry::SchemaDescriptors` to
//! `mcap::Writer::add_schema` and `mcap::Writer::add_channel`.
