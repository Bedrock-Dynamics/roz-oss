//! Phase 26 OBS-01: `TaskLifecycleSink` broadcast + emit helper (Wave 3 plan).
//!
//! STUB — implemented by a later wave. Kept as a doc-only module so the
//! `observability` barrel in `mod.rs` compiles. The Wave 3 plan replaces
//! this file with the broadcast sink that fans task-lifecycle transitions
//! out to subscribed `WriterActor`s.
