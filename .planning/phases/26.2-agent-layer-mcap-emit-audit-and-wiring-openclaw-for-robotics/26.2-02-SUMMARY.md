---
phase: 26.2-agent-layer-mcap-emit-audit-and-wiring-openclaw-for-robotics
plan: 02
subsystem: observability
tags: [observability, agent-loop, trait-seam, sessionevent, mcap-emit]
wave: 1
requires:
  - "crates/roz-core/src/session/event.rs::SessionEvent"
  - "crates/roz-core/src/checkpoint_signal.rs (precedent pattern)"
provides:
  - "roz_core::agent_event_hook::AgentEventHook"
  - "roz_core::agent_event_hook::NoopAgentEventHook"
  - "AgentLoop::with_agent_event_hook builder"
  - "AgentLoop.agent_event_hook field (pub(super), defaults to Noop)"
affects:
  - "crates/roz-core/src/lib.rs (module barrel — new pub mod)"
  - "crates/roz-agent/src/agent_loop/mod.rs (field, default, builder)"
tech-stack:
  added: []
  patterns:
    - "Phase 24 CheckpointSignal trait-object pattern (verbatim clone)"
    - "Arc<dyn Trait> field + with_* builder + Noop default"
key-files:
  created:
    - "crates/roz-core/src/agent_event_hook.rs"
  modified:
    - "crates/roz-core/src/lib.rs"
    - "crates/roz-agent/src/agent_loop/mod.rs"
decisions:
  - "Mirror Phase 24 CheckpointSignal verbatim — single on_agent_event(SessionEvent) method instead of per-variant"
  - "Zero emit call sites added — deferred to Plan 04 (Wave 2) per plan scope"
  - "NoopAgentEventHook default keeps behavior identical for all existing tests"
metrics:
  completed: 2026-04-22
  tasks: 2
  commits:
    - "271426d: feat(26.2-02): add AgentEventHook trait + NoopAgentEventHook in roz-core"
    - "f33ded3: feat(26.2-02): wire AgentEventHook field + builder into AgentLoop"
---

# Phase 26.2 Plan 02: AgentEventHook Trait Seam Summary

Install the `AgentEventHook` trait seam in `roz-core` and wire a default-Noop field + `with_agent_event_hook` builder into `AgentLoop`, cloning the Phase 24 `CheckpointSignal` pattern verbatim. No emit call sites added — Plan 04 owns the call-site wiring.

## What Was Built

### New file: `crates/roz-core/src/agent_event_hook.rs` (40 lines)

- `pub trait AgentEventHook: Send + Sync` with single method `fn on_agent_event(&self, event: SessionEvent)`
- `pub struct NoopAgentEventHook` + `impl AgentEventHook for NoopAgentEventHook` default implementation
- Module doc-comment references the Phase 24 `CheckpointSignal` precedent and Plan 04's target emit sites
- SessionEvent imported as `crate::session::event::SessionEvent`

### Modified: `crates/roz-core/src/lib.rs`

- Added `pub mod agent_event_hook;` in alphabetical order between `pub mod adapter;` and `pub mod auth;`

### Modified: `crates/roz-agent/src/agent_loop/mod.rs`

- Added import: `use roz_core::agent_event_hook::{AgentEventHook, NoopAgentEventHook};` adjacent to existing `use roz_core::checkpoint_signal::...` line
- Added field `pub(super) agent_event_hook: std::sync::Arc<dyn AgentEventHook>` immediately after the existing `checkpoint_signal` field, with matching doc-comment style
- Added default `agent_event_hook: std::sync::Arc::new(NoopAgentEventHook)` in the `Self { ... }` literal inside `AgentLoop::new`
- Added `#[must_use] pub fn with_agent_event_hook(mut self, hook: std::sync::Arc<dyn AgentEventHook>) -> Self` builder immediately after the existing `with_checkpoint_signal` builder

## Scope Boundary Respected

**Zero emit call sites added.** `git diff --name-only crates/roz-agent/src/agent_loop/core.rs crates/roz-agent/src/agent_loop/dispatch.rs` returns empty. Call-site wiring is deferred to Plan 04 (Wave 2) per plan objective.

## Behavior Change

**None.** Every `AgentLoop` instance now carries a `NoopAgentEventHook` by default; `on_agent_event` is an empty method so existing tests and runtime paths behave identically. All 713 `roz-agent` lib tests pass unchanged.

## Verification Results

| Check | Scope | Result |
|-------|-------|--------|
| `cargo check --workspace` | workspace | pass |
| `cargo fmt -p roz-core -p roz-agent --check` | scoped | pass |
| `cargo clippy -p roz-core -- -D warnings` | roz-core | pass |
| `cargo clippy -p roz-agent -- -D warnings` | roz-agent | pass |
| `cargo clippy --workspace --lib -- -D warnings` | workspace lib | pass |
| `cargo test -p roz-agent --lib --no-fail-fast` | roz-agent unit | 713 passed, 0 failed, 4 ignored |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Lint] Restructured doc comments to satisfy `clippy::too_long_first_doc_paragraph`**
- **Found during:** Task 1 `cargo clippy -p roz-core -- -D warnings`
- **Issue:** Workspace lint config enables `clippy::pedantic` + `clippy::nursery` at warn level with `-D warnings`. The module-level `//!` and trait `///` first paragraphs (copied from the plan's example block) exceeded the 4-line nursery threshold for `too_long_first_doc_paragraph`.
- **Fix:** Split each long first paragraph into a single-line headline followed by a blank line and an expanded paragraph. Trait/Noop/field semantics unchanged; only doc layout adjusted. No `#[allow(clippy::...)]` suppressions added.
- **Files modified:** `crates/roz-core/src/agent_event_hook.rs`
- **Commit:** Included in 271426d (Task 1 commit; fix applied before commit)

## Deferred Issues

**Pre-existing workspace `fmt` drift in `crates/roz-cli/src/commands/session.rs`** — `cargo fmt --check` at workspace scope flags one macro call in roz-cli that is unrelated to this plan's scope (not in the `files_modified` list). Logged to `.planning/phases/26.2-.../deferred-items.md`. Scoped `cargo fmt -p roz-core -p roz-agent --check` passes clean.

## Threat Flags

None. Trait is routed-to-runtime only; default `NoopAgentEventHook` is zero-op; no new trust boundary crossed (per plan threat register T-26.2-02 / T-26.2-03 "accept" dispositions).

## Acceptance Criteria — All Met

- [x] `crates/roz-core/src/agent_event_hook.rs` exists (40 lines, > 30 min)
- [x] Contains `pub trait AgentEventHook`
- [x] Contains `pub struct NoopAgentEventHook`
- [x] Contains `impl AgentEventHook for NoopAgentEventHook`
- [x] Contains `fn on_agent_event` exactly 2 times (trait def + Noop impl)
- [x] Uses `use crate::session::event::SessionEvent`
- [x] `pub mod agent_event_hook` added to `crates/roz-core/src/lib.rs`
- [x] `use roz_core::agent_event_hook::` in `agent_loop/mod.rs`
- [x] `pub(super) agent_event_hook: std::sync::Arc<dyn AgentEventHook>` field
- [x] `fn with_agent_event_hook` builder exists
- [x] `std::sync::Arc::new(NoopAgentEventHook)` default in constructor
- [x] W4 constructor-drift guard: `checkpoint_signal: std::sync::Arc::new(NoopCheckpointSignal)` still present adjacent (verified line 110-111)
- [x] No clippy suppressions in new file
- [x] No changes to `core.rs` or `dispatch.rs`
- [x] Plan 04 call-site scope preserved

## Self-Check: PASSED

**Files exist:**
- FOUND: `crates/roz-core/src/agent_event_hook.rs`
- FOUND: `crates/roz-core/src/lib.rs` (modified — `pub mod agent_event_hook` present at line 2)
- FOUND: `crates/roz-agent/src/agent_loop/mod.rs` (modified — import, field, default, builder all present)

**Commits exist in git log:**
- FOUND: 271426d
- FOUND: f33ded3
