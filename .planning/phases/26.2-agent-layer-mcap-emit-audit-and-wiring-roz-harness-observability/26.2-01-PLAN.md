---
phase: 26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md
autonomous: true
requirements: []
tags: [observability, mcap, audit, sessionevent]

must_haves:
  truths:
    - "Coverage matrix present in 26.2-CONTEXT.md with exactly 42 rows (one per SessionEvent variant)"
    - "Every row has a status marker (✅ / ⚠️ / ❌) per D-04 classification"
    - "Every row has a Wiring action column populated: 'verify' | 'add emit at <file:line>' | 'defer (D-15)' | 'proto-lossy; D-17 defer'"
  artifacts:
    - path: ".planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md"
      provides: "Coverage Matrix section with 42-row table"
      contains: "## Coverage Matrix (Phase 26.2 Wave 1 deliverable per D-01)"
  key_links:
    - from: "26.2-CONTEXT.md Coverage Matrix"
      to: "crates/roz-core/src/session/event.rs::SessionEvent enum"
      via: "variant name cross-reference"
      pattern: "SessionEvent::"
---

<objective>
Append a ## Coverage Matrix section to 26.2-CONTEXT.md that enumerates all 42 SessionEvent variants × emit site × proto mapper arm × status × wiring action. Soft cross-cut: reinforces OBS-01/02 fidelity (every agent event is first-class in MCAP).

Purpose: D-01 mandates audit-first workflow. The matrix is the hand-off from audit to Plan 04 (wiring gap closure). Plans 04/05/06 all read this matrix to determine which emits to add and which variants to assert on.

Output: `.planning/phases/26.2-.../26.2-CONTEXT.md` with a new appended section (does NOT replace existing content).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md
@.planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-RESEARCH.md
@.planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-PATTERNS.md

<interfaces>
<!-- RESEARCH.md already contains the fully-populated 42-row matrix at lines 114-208. -->
<!-- This plan copies that matrix into CONTEXT.md with one additional column ("Wiring action") -->
<!-- populated per RESEARCH.md §"Wiring Gap Analysis" (lines 555-613). -->

Source of truth: `crates/roz-core/src/session/event.rs::SessionEvent` enum (lines 207-471, 42 variants).
Mapper source of truth: `crates/roz-server/src/grpc/event_mapper.rs::typed_event_proto` (lines 68-493).
Proto-lossy variants (D-17): SafetyViolation (enum line 346), RecoveryPending (line 361) — both at mapper line 491 return None.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Coverage Matrix section to 26.2-CONTEXT.md</name>
  <files>.planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md</files>
  <read_first>
    - .planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md (current state — you are APPENDING, not replacing)
    - .planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-RESEARCH.md (lines 106-230 — the 42-row enum table + roll-up statistics + D-10 gap analysis are the source data)
    - .planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-RESEARCH.md (lines 555-613 — Wiring Gap Analysis dictates the "Wiring action" column values)
    - crates/roz-core/src/session/event.rs (spot-check 5 variants against the current main; RESEARCH.md was generated 2026-04-21 — if event.rs has drifted since, the matrix must reflect main)
    - .planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-PATTERNS.md (lines 760-781 — Coverage Matrix Embedding Pattern section prescribes header + column layout verbatim)
  </read_first>
  <action>
Append a new section to the END of `.planning/phases/26.2-.../26.2-CONTEXT.md`. Do not modify the existing <domain>, <decisions>, <canonical_refs>, <code_context>, <specifics>, <deferred> blocks.

The appended section MUST have this exact header and include all four sub-parts below:

```markdown
## Coverage Matrix (Phase 26.2 Wave 1 deliverable per D-01)

**Generated:** 2026-04-21
**Source of truth:** `crates/roz-core/src/session/event.rs::SessionEvent` enum (lines 207-471; 42 variants verified by grep against main branch).
**Mapper source of truth:** `crates/roz-server/src/grpc/event_mapper.rs::typed_event_proto` (lines 68-493).

Legend: ✅ production-emitted with typed proto | ⚠️ partial (narrow path, test-only, or proto-lossy) | ❌ no production emit | N/A not applicable

### Rollup

| Status | Count | Action in Phase 26.2 |
|--------|-------|----------------------|
| ✅ full | 18 | verify in new integration test |
| ⚠️ partial | 5 | see per-row note (remote-only / lossy / tool-local) |
| ❌ missing | 19 | 5 WIRED in Plan 04 (D-10 + D-14); 14 DEFERRED per D-15 |

### Variants (42 rows, sorted by classification then alphabetically)
```

Then a Markdown table with THESE EXACT COLUMNS:
`| # | Variant | Classification | Production emit site(s) | Proto mapper arm | Has typed payload? | Status | Wiring action (26.2) |`

Populate ALL 42 rows by copying RESEARCH.md lines 166-208 verbatim, adding a final "Wiring action (26.2)" column per this mapping (derived from RESEARCH.md §"Wiring Gap Analysis" lines 555-613 and D-10 gap analysis lines 220-234):

- Variants with Status ✅: "verify in Plan 05 integration test" (or "verify; emitted via SC5 approval loop" for ApprovalRequested/ApprovalResolved)
- `ToolCallRequested` (⚠️ remote only): "add in-process emit — Plan 04 Gap 3"
- `ModelCallCompleted` (❌): "add emit — Plan 04 Gap 1 (D-10 BLOCKING)"
- `ReasoningTrace` (❌): "add emit — Plan 04 Gap 2 (D-10 optional; wire per advisor note)"
- `ToolCallStarted` (❌): "add emit — Plan 04 Gap 4 (D-10 BLOCKING)"
- `ToolCallFinished` (❌): "add emit — Plan 04 Gap 5 (D-10 BLOCKING)"
- `ToolUnavailable` (❌): "defer (D-15 — not D-10 required; tool_category::NotRegistered site is small but out of phase scope)"
- `VerificationStarted` / `VerificationFinished` (❌): "defer (D-15 — controller verification is Phase 26.x)"
- `ResumeSummaryReady` (❌): "defer (D-15 — infrastructure)"
- `TelemetryStatusChanged` / `TrustPostureChanged` (❌): "defer (D-15 — telemetry; sync_* functions update state silently)"
- `SafePauseCleared` (❌): "defer (D-15 — no clearer site today)"
- `ControllerShadowStarted` / `ControllerRolledBack` (❌): "defer (D-15 — Phase 26.x controller scope)"
- `SafetyIntervention` (❌): "defer (D-15 — copper→session plumbing out of phase scope)"
- `MemoryRead` / `MemoryWrite` (❌): "defer (D-14 eligible but not D-10 required; re-evaluate in Plan 04 if <30 LOC each)"
- `SensorRepositioned` (❌): "defer (D-15 — no perception tool emits this yet)"
- `ContactStateChanged` (❌): "defer (D-15 — telemetry)"
- `FeedbackReceived` (❌): "defer (D-15 — no feedback-collection tool emits this yet)"
- `SafetyViolation` (⚠️ lossy proto): "emits; typed_event=None — D-17 defer proto fix to targeted follow-on"
- `RecoveryPending` (⚠️ lossy proto): "emits; typed_event=None — D-17 defer proto fix to targeted follow-on"
- `ControllerLoaded` / `ControllerPromoted` (⚠️ tool-specific): "verify; emits via promote_controller CLI tool only"
- `ThinkingDelta` (⚠️ provider-conditional): "verify in Plan 05 (mock provider emits ThinkingDelta)"

After the table, append these three subsections verbatim:

```markdown
### D-10 required variant coverage (post-Plan 04)

| D-10 required | Current emit? | Post-Plan 04 emit? | Verifier |
|---------------|---------------|--------------------|----------|
| TurnStarted | ✅ session_runtime/mod.rs:1242 | ✅ (no change) | Plan 05 assert |
| ModelCallCompleted | ❌ | ✅ (Plan 04 Gap 1) | Plan 05 assert |
| ReasoningTrace | ❌ | ✅ (Plan 04 Gap 2; WARN-not-BLOCK per D-10) | Plan 05 tolerant assert |
| ToolCallRequested (in-process) | ❌ | ✅ (Plan 04 Gap 3) | Plan 05 assert |
| ToolCallStarted | ❌ | ✅ (Plan 04 Gap 4) | Plan 05 assert |
| ToolCallFinished | ❌ | ✅ (Plan 04 Gap 5) | Plan 05 assert |
| TextDelta | ✅ session_runtime/mod.rs:1010 | ✅ (no change) | Plan 05 assert |
| TurnFinished | ✅ session_runtime/mod.rs:1375 | ✅ (no change) | Plan 05 assert |

### D-17 proto-lossy variants (deferred to targeted follow-on)

- `SafetyViolation` — envelope emits from `roz-worker/src/dispatch.rs:513`; `event_mapper.rs:491` returns `None`. typed_event payload is lost at proto encode. NOT FIXED in 26.2 per D-17.
- `RecoveryPending` — envelope emits from `roz-worker/src/recovery.rs:78`; same mapper.rs:491 path. NOT FIXED in 26.2 per D-17.

### D-15 deferred variants (out of phase scope)

ToolUnavailable, VerificationStarted, VerificationFinished, ResumeSummaryReady, TelemetryStatusChanged, TrustPostureChanged, SafePauseCleared, ControllerShadowStarted, ControllerRolledBack, SafetyIntervention, SensorRepositioned, ContactStateChanged, FeedbackReceived, MemoryRead, MemoryWrite (D-14-eligible; re-evaluated in Plan 04).
```

No other changes. Leave a trailing blank line at EOF.
  </action>
  <verify>
    <automated>grep -c '^| [0-9]\+ |' .planning/phases/26.2-*/26.2-CONTEXT.md | awk '{ if ($1 >= 42) exit 0; else { print "FAIL: expected >=42 rows, got " $1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Coverage Matrix' .planning/phases/26.2-*/26.2-CONTEXT.md` returns 1
    - `grep -c '^| [0-9]\+ |' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 42 (one per variant; the rollup table uses non-numeric leading cells so won't collide)
    - `grep -c 'ModelCallCompleted' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 2 (matrix row + D-10 required table row + possibly D-10 gap table)
    - `grep -c 'Plan 04 Gap' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 5 (Gaps 1-5)
    - `grep -c 'defer (D-15' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 10 (deferred variants per D-15 plus D-15 deferred subsection)
    - `grep -c 'D-17 defer' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 2 (SafetyViolation + RecoveryPending rows)
    - `grep -c '| ✅' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 18 (matches the rollup count)
    - `grep -c '| ❌' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 19 (matches the rollup count)
    - Existing sections untouched: `grep -c '^<decisions>' .planning/phases/26.2-*/26.2-CONTEXT.md` returns 1; `grep -c '^<canonical_refs>' .planning/phases/26.2-*/26.2-CONTEXT.md` returns 1
  </acceptance_criteria>
  <done>
    - 26.2-CONTEXT.md has a new `## Coverage Matrix` section appended at EOF
    - Matrix contains exactly 42 numbered variant rows (D-02 source-of-truth count)
    - Each row has a populated Wiring action column
    - D-10 required variants, D-17 proto-lossy variants, and D-15 deferred variants each have their own subsection
    - No existing CONTEXT.md content modified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan modifies only a planning document (.planning/phases/.../26.2-CONTEXT.md). No runtime surface, no network surface, no user input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.2-01 | I | .planning/ directory | accept | Planning docs are intentionally untracked by git (per user global memory rule "Planning docs not tracked in git"); no risk of accidental commit of sensitive data. File contains only public-architecture audit data — no secrets, no PII. |
</threat_model>

<verification>
Run after all tasks:
- `grep -c '^| [0-9]\+ |' .planning/phases/26.2-*/26.2-CONTEXT.md` returns at least 42
- Existing ROADMAP/STATE files unchanged
- No .rs / .toml / .sql / .proto files modified by this plan
</verification>

<success_criteria>
- 26.2-CONTEXT.md contains the Coverage Matrix with 42 variant rows, D-10 coverage subtable, D-17 deferred list, D-15 deferred list
- Audit data is accurate against main (spot-checked 5 variants)
- Plan 04 can read the matrix to determine which emits to add
- Plan 05 can read the matrix to determine which variants to assert on
</success_criteria>

<output>
After completion, create `.planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-01-SUMMARY.md` documenting:
- Matrix row count (42)
- Variant status tallies (✅/⚠️/❌ counts)
- List of variants flagged for Plan 04 wiring (the 5 D-10 gaps)
- List of variants deferred per D-15 / D-17
- Any drift detected between RESEARCH.md's 2026-04-21 snapshot and main (if any variants were added/removed)
</output>
