---
phase: 26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability
plan: 06
subsystem: observability
tags: [observability, integration-test, mcap, sc5, fixture, agent-turn, regression-fence]
wave: 4
requires:
  - "crates/roz-server/tests/mcap_agent_session_live.rs (Plan 05 — construction reference)"
  - "crates/roz-agent/src/model/mock_provider.rs::mock_provider_v1 (Plan 03 + Plan 05 stateful fix)"
  - "crates/roz-agent/src/session_runtime::SessionRuntimeEventHook (Plan 04)"
  - "crates/roz-core/src/agent_event_hook::AgentEventHook (Plan 02)"
  - "crates/roz-server/src/observability/ingest_cloud::spawn_cloud_ingestors"
  - "crates/roz-server/src/observability/ingest_cloud::emit_session_event_for_tests (Plan 26-11)"
provides:
  - "SC5 regression fence now covers the agent-turn path via production emit chain (D-11 gap closed)"
  - "Lower-bound message-count assertions that tolerate non-deterministic agent-turn extras (D-12)"
affects:
  - "crates/roz-server/tests/export_roundtrip.rs (+451 / -23 lines)"
tech-stack:
  added: []
  patterns:
    - "SC5 fixture extension pattern: in-place agent-turn block between approvals and task-lifecycle"
    - "await_turn_drain deterministic broadcast barrier inlined per Plan 05 convention (REVIEWS.md M3)"
    - "Lower-bound (>=) assertions replace exact-eq for counts that include non-deterministic runtime extras"
    - "Post-decode HashSet<event_type> presence check for D-10 BLOCKING variants"
key-files:
  created:
    - ".planning/phases/26.2-.../26.2-06-SUMMARY.md (this file)"
  modified:
    - "crates/roz-server/tests/export_roundtrip.rs"
decisions:
  - "Inline copy of await_turn_drain + TestStreamingExecutor (option a per plan) — kept SC5 self-contained like Plan 05. No new cross-test dep, no roz-test surface change."
  - "Used roz_agent::model::mock_provider_v1 (REVIEWS.md H1) — roz-server test-helpers feature already transitively activates roz-agent/test-helpers via Plan 05's Cargo.toml edit, so import resolved without Cargo.toml changes."
  - "Hook constructed via runtime.event_emitter() (REVIEWS.md L1) — no API churn."
  - "Drain barrier is await_turn_drain, not tokio::time::sleep (REVIEWS.md M3) — matches Plan 05 semantics verbatim."
  - "Grand-total assertion switched to lower-bound (assert!(count >= min_expected)) — session events, log lines, and total count all accept extras above the D-10 floor."
  - "Post-decode presence check uses canonical event_type strings from crates/roz-core/src/session/event.rs:137+ (turn_started, model_call, tool_call_requested, tool_call_started, tool_call_finished, turn_finished)."
  - "ReasoningTrace tolerated absent (eprintln WARN only) — deferred to Phase 26.3 per REVIEWS.md M1."
  - "Agent-turn block placed between approval loop and task-lifecycle loop so events interleave naturally inside the MCAP without requiring new channel constants."
metrics:
  duration: "~30 min"
  tasks: 1
  completed: 2026-04-22
  commits:
    - "004f5e9: test(26.2-06): extend SC5 fixture with scripted agent-turn via mock_provider_v1"
requirements: [OBS-01]
---

# Phase 26.2 Plan 06: SC5 Agent-Turn Extension Summary

Extend the existing Phase 26 SC5 fixture at `crates/roz-server/tests/export_roundtrip.rs` to drive ONE scripted agent turn through the production mock-provider → AgentLoop → SessionRuntime → EventEmitter → broadcast → spawn_cloud_ingestors → MCAP chain, closing D-11's fidelity gap. SC5 now fails on regressions of Plan 04's emit wiring (Gaps 1, 3, 4, 5), not just regressions of the approvals + task-lifecycle stubs.

## What Shipped

### Task 1 — SC5 fixture extension

**`crates/roz-server/tests/export_roundtrip.rs`** — +451 / -23 lines.

**Diff summary:**
- **Lines 1-50 (docstring):** expanded module docs to enumerate the new agent-turn flow and lower-bound assertions.
- **Lines 57-95 (imports):** added `std::collections::HashSet`, `std::sync::Arc`, `std::time::Duration`, plus 10 `roz_agent::*` / `roz_core::*` / `roz_server::*` / `tokio::sync::*` imports for the agent-turn drive (mock_provider_v1, SessionRuntime, SessionRuntimeEventHook, AgentEventHook, spawn_cloud_ingestors, new_task_lifecycle_sink, broadcast::Receiver, mpsc, etc.).
- **Lines 108-136 (constant):** added `AGENT_TURN_SESSION_EVENTS_MIN: u64 = 6` with full rustdoc capturing Plan 05's empirical DrainReport baseline (all 6 D-10 BLOCKING variants, ReasoningTrace=false, total_events=18).
- **Lines 275-402 (agent-turn block):** new scoped block `{ ... }` between approval loop and task-lifecycle loop. Builds `SessionRuntime` + `ToolDispatcher{MockToolExecutor("hello_world", ToolCategory::Pure)}` + `AgentLoop{mock_provider_v1, dispatcher, SafetyStack::empty, MockSpatialContextProvider::empty}.with_agent_event_hook(SessionRuntimeEventHook::new(runtime.event_emitter()))`. Runs one turn via `runtime.run_turn_streaming(turn_input, Some(message_id), &mut executor)` and barriers on `await_turn_drain(drain_rx, 5s)`.
- **Lines 482-527 (assertions):** replaced exact-eq `session_events_count == APPROVAL_PAIRS*2`, `log_count >= expected_session_events`, and `count == expected` with lower-bound `>=` variants using `min_expected_session_events = APPROVAL_PAIRS*2 + AGENT_TURN_SESSION_EVENTS_MIN` (= 16) and `min_expected = TOTAL_TELEMETRY*2 + TOOL_CALL_TRIPLETS*3 + APPROVAL_PAIRS*2*2 + AGENT_TURN_SESSION_EVENTS_MIN*2 + TASK_LIFECYCLE_TRANSITIONS` (= 3112).
- **Lines 531-588 (post-assertion):** new `HashSet<String>` decode over `/roz/session/events` channel via `roz_v1::SessionEventEnvelope::decode`. Asserts presence of `turn_started`, `model_call`, `tool_call_requested`, `tool_call_started`, `tool_call_finished`, `turn_finished`, `approval_requested`, `approval_resolved`. ReasoningTrace absent emits WARN only (REVIEWS.md M1 defer).
- **Lines 617-707 (await_turn_drain):** inline copy of Plan 05's drain helper — concrete `broadcast::Receiver<EventEnvelope>` type, 5-second `tokio::time::timeout`-bounded loop, 8x `tokio::task::yield_now` tail to scheduler-tick the cloud ingestor's background enqueues.
- **Lines 718-763 (TestStreamingExecutor):** inline copy of Plan 05's executor — clones AgentInput per turn, spins up fresh mpsc channels, delegates to `AgentLoop::run_streaming_seeded`, maps `AgentOutput` → `TurnOutput` and `AgentLoop::Err` → `TurnExecutionFailure(RuntimeFailureKind::ModelError, ...)`.

**Preserved verbatim:**
- Telemetry fixture block (lines 187-212) with pinned `log_time_ns = now_ns + i * 20_000_000`.
- Tool-call triplet loop, approval pair loop, task-lifecycle loop.
- Quaternion non-identity decode assertion (lines 590-599).
- Archive row status/digest/size assertion (lines 601-614).
- `session_events_count > 0` anti-regression (line 475-480).

**Cargo.toml:** untouched (`git diff --name-only crates/roz-server/Cargo.toml` returns empty). Plan 05's `test-helpers = ["roz-agent/test-helpers"]` propagation is what makes the `roz_agent::model::mock_provider_v1` import resolve here; no new feature wiring needed.

## Observed session-events count during the run

The test passed end-to-end against testcontainers Postgres. Because the test uses lower-bound assertions (`>=`), the exact count is not printed directly, but Plan 05's measurement device shows the agent turn contributes exactly the 18 events reported below. Combined with SC5's 10 approval events → `session_events_count` lands around 28, well above the floor of 16.

## Whether ReasoningTrace was observed

**No** — and this is correct per REVIEWS.md M1 (ReasoningTrace emit deferred to Phase 26.3 because `ReasoningTraceBuilder::new` requires a `turn_index` that `AgentLoop` does not yet carry). The `WARN:` line printed during the run is informational, not a failure.

## Drain report observed during the real run

```text
SC5 agent-turn drain report: DrainReport {
    saw_turn_started: true,
    saw_model_call: true,
    saw_tool_call_requested: true,
    saw_tool_call_started: true,
    saw_tool_call_finished: true,
    saw_turn_finished: true,
    saw_reasoning_trace: false,
    total_events: 18,
}
```

All 6 D-10 BLOCKING variants observed on the broadcast within ~1.3 seconds of test start (same wall-clock profile as Plan 05's standalone test). Tail of 8x `yield_now` gave the cloud ingestor's background task enough scheduler ticks to enqueue every event into MCAP before SC5's `Finalize` send downstream.

## Turn result observed during the real run

```text
SC5 agent-turn turn_result: Completed(TurnOutput {
    assistant_message: "Done.",
    tool_calls_made: 2,
    input_tokens: 84,             // 42 * 2 (stateful mock: call #1 + call #2)
    output_tokens: 26,            // 13 * 2
    cache_read_tokens: 0,
    cache_creation_tokens: 0,
    messages: [
        Message { role: User, parts: [Text { text: "Please say hello." }] },
        Message { role: Assistant, parts: [
            Thinking { thinking: "I should greet the user.", signature: "" },
            ToolUse { id: "toolu_mock_1", name: "hello_world",
                      input: Object {"name": String("world")} },
        ]},
        Message { role: User, parts: [ToolResult {
            tool_use_id: "toolu_mock_1", name: "hello_world",
            content: "{\"output\":{\"greeting\":\"hello, world\"},\"error\":null}",
            is_error: false,
        }]},
        Message { role: Assistant, parts: [Text { text: "Done." }] },
    ],
})
```

Confirms the stateful 2-call mock from Plan 05 (call #1 → ToolUse+stop_reason=tool_use, call #2 → Text "Done."+stop_reason=end_turn) correctly fires Plan 04's dispatch-level Requested/Started/Finished emit sites.

## REVIEWS.md compliance confirmations

- **H1 (import path):** `use roz_agent::model::mock_provider_v1;` present at line 70. `use roz_test::mock_provider_v1;` absent. ✓
- **L1 (event emitter accessor):** `SessionRuntimeEventHook::new(runtime.event_emitter())` at line 349. `runtime.event_emitter_clone` string absent (one comment mentions `event_emitter_clone` at line 348 as what NOT to use — the strict grep `grep -q 'runtime.event_emitter_clone'` exits 1). ✓
- **M3 (drain barrier):** `await_turn_drain` helper defined + called with 5-second timeout at line 396. `tokio::time::sleep` appears only in the unchanged DB-row poll loop (line 430) AFTER `Finalize` was already sent — not as a drain barrier. The agent-turn block uses the deterministic helper. ✓
- **M1 (ReasoningTrace defer):** tolerant check at lines 582-588 (WARN-only if absent); matches Plan 05 behavior. ✓

## Docker availability during execution

**Yes** — Docker Desktop 29.3.1 was available, testcontainers `pg_container` bootstrapped successfully, the end-to-end test PASSED:

```text
$ cargo test -p roz-server --features test-helpers --test export_roundtrip \
    -- --ignored --test-threads=1

running 1 test
test sc5_30s_fixture_roundtrips_via_mcap_message_stream ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out;
  finished in 1.29s
```

## SessionRuntime construction approach

**Inline** — same as Plan 05. `SessionConfig` literal is ~13 lines, plus `runtime.subscribe_events()` (x2), dispatcher + tool registration (~7 lines), AgentLoop + hook (~7 lines), AgentInput + TurnInput (~10 lines), executor + run_turn_streaming (~6 lines). Total agent-turn block ~128 lines including comments. No shared `SessionRuntime::for_tests` helper needed; the plan predicted this as option-a and Plan 05 confirmed it as the canonical form.

## Proto import path

Used **`roz_server::grpc::roz_v1::SessionEventEnvelope`** via `use roz_server::grpc::roz_v1;` + `roz_v1::SessionEventEnvelope::decode(msg.data.as_ref())`. Mirrors Plan 05's resolution of REVIEWS.md S1.

## Construction-seam discoveries that differed from Plan 05

**None** — the construction pattern was a verbatim copy. The only non-obvious seam was re-using SC5's existing writer `tx` (from `spawn_writer` at line 174) as the `spawn_cloud_ingestors` sender, so the agent-turn events land in the SAME MCAP file as the telemetry/tool-call/approval/task-lifecycle events. This is the correct production behavior (one writer per session) and was already implied by SC5's structure.

## Deviations from Plan

None. Plan 06 executed exactly as written. Plan 05's Rule-1 MockProviderV1 fix (stateful 2-call counter) had already unblocked the tool-dispatch emit sites, so no additional auto-fixes were needed.

## Threat Flags

No new threat surface. Every assertion reads MCAP bytes already written inside the per-session archive — no new network boundary, no new user-input surface. Testcontainers Postgres ephemeral (`std::mem::forget(guard)` keeps it alive only for test process). Mock provider still behind `test-helpers` feature; no production-reachable path.

Threat register dispositions from plan frontmatter all respected:
- T-26.2-13 (info disclosure in agent-turn content): accepted — only literal test strings ("I should greet the user.", "world", "Done.").
- T-26.2-14 (feature-flag inheritance): accepted — Plan 05 already wired propagation at Cargo.toml line 22; SC5 `required-features = ["test-helpers"]` at line 110 enforces gating.
- T-26.2-15 (lower-bound brittleness): mitigated — lower-bounds chosen empirically from Plan 05, not guessed; ReasoningTrace explicitly excluded from the floor.
- T-26.2-17 (drain livelock): mitigated — `await_turn_drain` bounded by 5-second `tokio::time::timeout`.

## Acceptance Criteria Verification

| Check | Result |
|-------|--------|
| H1: `grep -q 'use roz_agent::model::mock_provider_v1'` | PASS (line 70) |
| H1: `grep -q 'use roz_test::mock_provider_v1'` (must exit 1) | PASS (absent) |
| `grep -q 'SessionRuntimeEventHook'` | PASS |
| L1: `grep -q 'runtime.event_emitter()'` | PASS (line 349) |
| L1: `grep -q 'runtime.event_emitter_clone'` (must exit 1) | PASS (absent) |
| `grep -q 'AGENT_TURN_SESSION_EVENTS_MIN'` | PASS |
| `grep -q 'mock_provider_v1()'` | PASS |
| `grep -q 'run_turn_streaming'` | PASS |
| `grep -q 'spawn_cloud_ingestors'` | PASS |
| M3: `grep -q 'await_turn_drain\|drain.saw_turn_finished'` | PASS (both present) |
| M3: no `tokio::time::sleep` as drain barrier in agent-turn block | PASS (only in unrelated DB poll loop post-Finalize) |
| All 6 canonical event_type strings asserted | PASS |
| `grep -q '"approval_requested"'` | PASS |
| Grand-total lower-bound: `grep -q 'assert!(\s*count\s*>='` | PASS |
| Session-events lower-bound: `grep -q 'session_events_count\s*>=\s*min_expected'` | PASS |
| Telemetry block unchanged: TOTAL_TELEMETRY + seen_tf | PASS |
| Quaternion decode preserved: copper_quat_to_foxglove | PASS |
| `cargo fmt -p roz-server --check` (this file only) | PASS (pre-existing fmt diffs in unrelated files remain) |
| `cargo clippy -p roz-server --all-targets --features test-helpers -- -D warnings` | PASS |
| `cargo clippy -p roz-server --all-targets -- -D warnings` (no flag) | PASS |
| `cargo check -p roz-server --features test-helpers --test export_roundtrip` | PASS |
| Cargo.toml untouched | PASS (git diff empty) |
| Docker available + test passes `--ignored --test-threads=1` | PASS (1 passed, 0 failed, 1.29s) |

## Self-Check: PASSED

**Files verified present:**
- FOUND: `crates/roz-server/tests/export_roundtrip.rs` (789 lines after edit, grew from 361)
- FOUND: `.planning/phases/26.2-.../26.2-06-SUMMARY.md` (this file)

**Commits verified in git log:**
- FOUND: `004f5e9` — test(26.2-06): extend SC5 fixture with scripted agent-turn via mock_provider_v1

**Workspace checks:**
- `cargo build --workspace`: pass
- `cargo clippy -p roz-server --all-targets --features test-helpers -- -D warnings`: pass
- `cargo clippy -p roz-server --all-targets -- -D warnings` (no flag): pass
- `cargo test -p roz-server --features test-helpers --test export_roundtrip -- --ignored --test-threads=1`: **1 passed** (end-to-end against testcontainers Postgres, drain report matches Plan 05 baseline)
