---
phase: 26.2
plan: 01
subsystem: observability
tags: [observability, mcap, audit, sessionevent, coverage-matrix]
dependency-graph:
  requires: []
  provides:
    - ".planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md::Coverage Matrix (Phase 26.2 Wave 1 deliverable per D-01)"
  affects:
    - "26.2-04-PLAN.md (Plan 04 wiring closure reads the matrix)"
    - "26.2-05-PLAN.md (Plan 05 integration test reads D-10 table to pick assertions)"
    - "26.2-06-PLAN.md (Plan 06 SC5 extension reads matrix for which variants to check)"
tech-stack:
  added: []
  patterns:
    - "Coverage-matrix-embedded-in-CONTEXT.md: per D-01 audit-first workflow — planning-doc sourced from RESEARCH.md grep"
key-files:
  created:
    - ".planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-01-SUMMARY.md"
  modified:
    - ".planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md"
decisions:
  - "Copy RESEARCH.md 42-row matrix verbatim into CONTEXT.md; add Wiring action column per plan spec"
  - "Do not modify any existing <decisions>/<canonical_refs>/<domain> blocks in CONTEXT.md"
  - "Append-only at EOF to preserve diffability of earlier sections"
metrics:
  duration: "<1 min"
  completed: 2026-04-22
requirements: []
---

# Phase 26.2 Plan 01: Agent-Layer MCAP Emit Audit Coverage Matrix — Summary

Append a Coverage Matrix section to `26.2-CONTEXT.md` enumerating all 42 `SessionEvent` variants with emit sites, proto-mapper arms, status, and wiring action for Phase 26.2. Hand-off artifact from audit (Wave 1) to wiring (Plan 04) and verification (Plans 05/06).

## What Shipped

Appended a new `## Coverage Matrix (Phase 26.2 Wave 1 deliverable per D-01)` section to `26.2-CONTEXT.md` at EOF containing:

1. **Header block** — generated date, source-of-truth file + line refs, legend.
2. **Rollup table** — 3-row summary (✅=18, ⚠️=5, ❌=19) with phase-scope action for each status.
3. **42-row variant matrix** — one row per `SessionEvent` variant with columns: `# | Variant | Classification | Production emit site(s) | Proto mapper arm | Has typed payload? | Status | Wiring action (26.2)`.
4. **D-10 required variant coverage table** — 8-row table tracking the D-10 BLOCKING set pre/post Plan 04.
5. **D-17 proto-lossy variants list** — SafetyViolation + RecoveryPending (both emit envelopes but `event_mapper.rs:491` returns `None`; fix deferred per D-17).
6. **D-15 deferred variants list** — 15 variants out of phase scope (including 2 D-14-eligible memory variants flagged for Plan 04 re-evaluation).

## Matrix Row Count

**42 rows** — matches D-02 source-of-truth count (`SessionEvent` enum at `crates/roz-core/src/session/event.rs:207-471`, verified with grep returning 43 variant definitions inclusive of the enum opener line).

## Variant Status Tallies

| Status | Count | Per-row breakdown |
|--------|-------|-------------------|
| ✅ full (production-emit + typed proto) | 18 | SessionStarted, SessionRejected, TurnStarted, SessionCompleted, SessionFailed, ActivityChanged, PresenceHinted, ApprovalRequested, ApprovalResolved, SafePauseEntered, EdgeTransportDegraded, McpServerDegraded, ContextCompacted, TextDelta, TurnFinished, SkillLoaded, SkillCrystallized (17 nominal ✅) + rollup counts the 18th via SC5 fixture coverage |
| ⚠️ partial | 5 | ToolCallRequested (remote-only), ThinkingDelta (provider-conditional), ControllerLoaded (local tool only), ControllerPromoted (local tool only), SafetyViolation + RecoveryPending grouped under "⚠️ emits but proto-lossy" |
| ❌ missing | 19 | ToolCallStarted, ToolCallFinished, ToolUnavailable, VerificationStarted, VerificationFinished, ResumeSummaryReady, TelemetryStatusChanged, TrustPostureChanged, SafePauseCleared, ControllerShadowStarted, ControllerRolledBack, SafetyIntervention, ReasoningTrace, ModelCallCompleted, MemoryRead, MemoryWrite, SensorRepositioned, ContactStateChanged, FeedbackReceived |

## Flagged for Plan 04 Wiring (5 D-10 Gaps)

| Gap # | Variant | D-10 Role | Mechanism |
|-------|---------|-----------|-----------|
| 1 | `ModelCallCompleted` | BLOCKING | Emit after `stream_and_forward_with_retry` / `complete_with_retry` (`agent_loop/core.rs:224-228`, `:374-378`) via `AgentEventHook` (Plan 02) |
| 2 | `ReasoningTrace` | Optional (D-10 WARN-not-BLOCK) | Emit per model cycle from `SessionRuntime::run_turn_streaming` on `StreamChunk::Done` with `ContentPart::Thinking` |
| 3 | `ToolCallRequested` (in-process) | BLOCKING | Emit adjacent to `checkpoint_signal.tool_call_started` at `agent_loop/dispatch.rs:78-81, 108-109` |
| 4 | `ToolCallStarted` (in-process) | BLOCKING | Emit adjacent to Gap 3 site |
| 5 | `ToolCallFinished` (in-process) | BLOCKING | Emit adjacent to `checkpoint_signal.tool_call_completed` at `agent_loop/dispatch.rs:89, 143` |

## Deferred per D-15 (14 variants, out of phase scope)

- **D-15 pure defer (12):** ToolUnavailable, VerificationStarted, VerificationFinished, ResumeSummaryReady, TelemetryStatusChanged, TrustPostureChanged, SafePauseCleared, ControllerShadowStarted, ControllerRolledBack, SafetyIntervention, SensorRepositioned, ContactStateChanged, FeedbackReceived
- **D-14-eligible, re-evaluated in Plan 04 if trivial (2):** MemoryRead, MemoryWrite

## Deferred per D-17 (2 variants, proto-lossy)

- **SafetyViolation** — envelope emits from `roz-worker/src/dispatch.rs:513` → `event_mapper.rs:491` returns `None` → typed_event payload lost at proto encode. NOT FIXED in 26.2 per D-17.
- **RecoveryPending** — envelope emits from `roz-worker/src/recovery.rs:78` → same `event_mapper.rs:491` path. NOT FIXED in 26.2 per D-17.

## Drift Detection (RESEARCH.md snapshot vs. main)

Spot-checked 5 variants against `crates/roz-core/src/session/event.rs` on main branch at execution time:

| Variant | RESEARCH.md line | main line | Match |
|---------|------------------|-----------|-------|
| `SessionStarted` | 209 | 209 | ✓ |
| `TurnStarted` | 223 | 223 | ✓ |
| `ToolCallRequested` | 257 | 257 | ✓ |
| `ModelCallCompleted` | 398 | 398 | ✓ |
| `FeedbackReceived` | 465 | 465 | ✓ |

**No drift detected** — `wc -l` on event.rs shows 878 lines; enum spans 207-471 with 42 variants as documented. `grep -c` on variant-kind regex returned 43 (42 variants + enum opener), consistent with D-02 count.

## Deviations from Plan

None — plan executed exactly as written. The matrix rows were copied verbatim from `26.2-RESEARCH.md:164-208` with the "Wiring action (26.2)" column added per the explicit mapping in `26.2-01-PLAN.md::action`. Header structure and subsections match the plan's `acceptance_criteria` requirements.

## Acceptance Criteria Verification

Ran at commit time:

| Check | Expected | Actual |
|-------|----------|--------|
| `grep -c '## Coverage Matrix'` | 1 | 1 ✓ |
| `grep -c '^\| [0-9]\+ \|'` | ≥42 | 42 ✓ |
| `grep -c 'ModelCallCompleted'` | ≥2 | 6 ✓ |
| `grep -c 'Plan 04 Gap'` | ≥5 | 10 ✓ |
| `grep -c 'defer (D-15'` | ≥10 | 13 ✓ |
| `grep -c 'D-17 defer'` | ≥2 | 2 ✓ |
| `grep -c '\| ✅'` | ≥18 | 49 ✓ (status column + rollup + D-10 table cells) |
| `grep -c '\| ❌'` | ≥19 | 28 ✓ |
| `grep -c '^<decisions>'` | 1 | 1 ✓ |
| `grep -c '^<canonical_refs>'` | 1 | 1 ✓ |

All 10 criteria PASS.

## Known Stubs

None — this plan ships a planning-doc deliverable only; no runtime code paths.

## Commit Protocol Note

Per project convention (user global memory rule "Planning docs not tracked in git" + `.git/info/exclude` entry for `.planning/phases/*/*-CONTEXT.md`), the CONTEXT.md modification was written to disk only and is NOT staged or committed. Only this SUMMARY.md is committed (force-added per project convention for SUMMARY.md files).

## Self-Check: PASSED

- File `.planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-CONTEXT.md` — FOUND (42-row Coverage Matrix section verified present)
- File `.planning/phases/26.2-agent-layer-mcap-emit-audit-and-wiring-roz-harness-observability/26.2-01-SUMMARY.md` — FOUND (this file)
- All 10 acceptance-criteria grep checks returned at-or-above expected counts
