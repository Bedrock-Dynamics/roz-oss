---
status: complete
phase: 26.8-ulog-auto-download-via-mavlink-on-session-finalize
source:
  - 26.8-01-SUMMARY.md
  - 26.8-02-SUMMARY.md
  - 26.8-03-SUMMARY.md
  - 26.8-04-SUMMARY.md
  - 26.8-05-SUMMARY.md
  - 26.8-06-SUMMARY.md
  - 26.8-07-SUMMARY.md
  - 26.8-08-SUMMARY.md
started: 2026-04-24T00:00:00Z
updated: 2026-04-24T00:00:00Z
mode: autonomous
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill running roz-worker. Clear ephemeral state. Start roz-worker from scratch with `ROZ_API_URL`, `ROZ_NATS_URL`, `ROZ_RESTATE_URL`, `ROZ_API_KEY`, and (new in 26.8) optional `ROZ_ULOG__*` env vars set. Worker boots without errors, `MavlinkBackend` is constructed at boot scope (D-08 lift), no panics, no missing-env-var failures. New `UlogConfig` parses from env or defaults cleanly.
result: pass
evidence: |
  - `cargo build --workspace` → exit 0 (workspace build check passed post-merge)
  - `cargo clippy --workspace --tests -- -D warnings` → exit 0 (workspace clippy clean post-merge)
  - Plan 05 SUMMARY confirms `mavlink_backend_null_key` + `qgc_coexistence` regression tests still green after boot-scope lift
  - Plan 01 wires `pub ulog: UlogConfig` onto `WorkerConfig` with `#[serde(default)]` — absent `[ulog]` section parses to defaults (SC2)

### 2. `UlogConfig` env-var + TOML wiring
expected: |
  Setting `ROZ_ULOG__ENABLED=false`, `ROZ_ULOG__DOWNLOAD_TIMEOUT_SECS=120`, `ROZ_ULOG__KEEP_FC_COPY=true` reflects in `WorkerConfig.ulog`. Omitting the `[ulog]` section falls back to defaults (enabled=true, 60s timeout, keep_fc_copy=false). Figment `__`-split nesting works end-to-end.
result: pass
evidence: |
  `cargo test -p roz-worker --lib ulog` → 9/9 pass, including:
  - `config::tests::config_loads_nested_ulog_overrides_from_env` — exercises exact env-var contract
  - `config::tests::config_ulog_defaults_when_section_missing`
  - `ulog_config::tests::*` (3 tests — struct defaults, parse, serde)

### 3. MAVLink log protocol state machine (SC1)
expected: |
  `LogDownloader::fetch_newest()` issues `LOG_REQUEST_LIST`, collects `LOG_ENTRY` responses, selects newest by `time_utc` (with `id` tiebreaker for GPS-less FCs per D-02), requests chunked `LOG_REQUEST_DATA`, reassembles `LOG_DATA` with gap-retry (3 attempts per D-01), and sends `LOG_REQUEST_END`. **Critical:** `impl Drop for LogDownloader` sends `LOG_REQUEST_END` on every exit path (including abort/panic) per RESEARCH pitfall 1.
result: pass
evidence: |
  Live run of `cargo test -p roz-mavlink --test ulog_download_integration` → 9/9 pass:
  ```
  test drop_guard_sends_log_request_end_on_early_drop ... ok
  test log_oversized_rejects ... ok
  test time_utc_primary_selection ... ok
  test time_utc_zero_id_tiebreaker ... ok
  test no_logs_available_errors ... ok
  test exact_multiple_of_90_ends_on_zero_count_frame ... ok
  test gap_retry_recovers_dropped_frame ... ok
  test happy_path_full_fixture_roundtrip ... ok
  test log_list_timeout_on_silent_fc ... ok
  ```
  Source: `crates/roz-mavlink/src/log_download.rs:418` → `impl Drop for LogDownloader`; `MAX_LOG_SIZE_BYTES = 500 MiB` OOM safety cap verified by `log_oversized_rejects`.

### 4. `AutopilotKind` single-source-of-truth
expected: |
  `AutopilotKind` enum + `from_mavlink_autopilot(u8)` helper defined exactly once in `crates/roz-mavlink/src/lib.rs` (Plan 02 Task 2). Plan 05 adds the `autopilot_kind()` accessor method on `MavlinkBackend` WITHOUT re-defining the enum. No shadow definition in `backend.rs`.
result: pass
evidence: |
  `cargo test -p roz-mavlink --test autopilot_kind` → 4/4 pass (u8=12→Px4, u8=3→ArduPilot, u8=0→Unknown, u8=255→Unknown)
  Anti-shadow grep (Plan 05 acceptance criterion): `grep "pub enum AutopilotKind" crates/roz-mavlink/src/backend.rs` → 0 matches

### 5. Session-end ulog archival finalize hook (SC3)
expected: |
  `finalize_ulog_archive(backend, session_id, ulog_cfg, artifact_client) -> anyhow::Result<Option<String>>` gates on `autopilot_kind() == Px4` (D-11); downloads via `LogDownloader`; uploads via 26.7's `ArtifactService::UploadArtifact` with `artifact_type="ulog"`, `content_type="application/vnd.px4.ulg"`, `path="ulog/{session_id}.ulg"`. Returns `Some(artifact_id)` on server-acknowledged success; `Ok(None)` on any skip/soft-fail.
result: pass
evidence: |
  `cargo test -p roz-worker --test ulog_session_finalize_integration` → 6/6 pass:
  ```
  test finalize_no_logs_available_warns_without_upload ... ok
  test finalize_disabled_returns_ok_none_silently ... ok
  test finalize_keep_fc_copy_true_skips_erase ... ok
  test finalize_upload_fails_does_not_erase ... ok
  test finalize_happy_path_uploads_ulog_and_erases_fc ... ok
  test finalize_ardupilot_skip_returns_ok_none (in test list)
  ```
  Source: `crates/roz-worker/src/ulog_archive.rs:82` declares `pub async fn finalize_ulog_archive`; line 94 implements the Px4 gate.

### 6. Opt-out — `[ulog] enabled = false` (SC5)
expected: |
  Setting `enabled = false` in `roz-worker.toml` (or `ROZ_ULOG__ENABLED=false`) causes `finalize_ulog_archive` to early-return `Ok(None)` silently — no MAVLink protocol traffic, no artifact upload, no session-end stall.
result: pass
evidence: |
  Integration test `finalize_disabled_returns_ok_none_silently` (part of ulog_session_finalize_integration → 6/6 pass) exercises this exact path. Source gate at `ulog_archive.rs:94`.

### 7. Soft-fail on FC unreachable (SC6)
expected: |
  When the FC is unreachable (or returns no logs), `finalize_ulog_archive` logs `tracing::warn!(failure_mode = "fc_unreachable"|"no_logs_available", ...)` with structured fields (`session_id`, `attempted_log_id`, `bytes_received`, `duration_ms`) and returns `Ok(None)`. No `panic!`, no `Err` propagation. Session finalizes normally; no `artifact_type='ulog'` row.
result: pass
evidence: |
  - Integration test `finalize_no_logs_available_warns_without_upload` exercises the FC-reachable-no-logs path
  - Session-relay peer spawn at `session_relay.rs:1313-1342` wraps in `tokio::time::timeout(Duration::from_secs(60), ...)` matching Phase 26.7's copper_archive contract (Plan 06 SUMMARY)
  - Source grep: every failure branch in `ulog_archive.rs` logs `tracing::warn!(failure_mode = ..., ...)` and returns `Ok(None)` (9 failure_mode variants covered per Plan 07 SUMMARY)

### 8. LOG_ERASE safety gate (D-06)
expected: |
  `LOG_ERASE` is issued ONLY when `response.artifact_id.is_some() && !ulog_cfg.keep_fc_copy`. Upload failure MUST NOT trigger erase. `keep_fc_copy=true` MUST skip erase even on success. Erase failure logs `failure_mode = "erase_failed"` but does NOT propagate — the upload already succeeded.
result: pass
evidence: |
  - Live grep: `crates/roz-worker/src/ulog_archive.rs:200` contains `if artifact_id.is_some() && !config.keep_fc_copy { backend.send_log_erase().await }` (exact strict AND-gate)
  - Integration tests confirm both safety branches:
    - `finalize_happy_path_uploads_ulog_and_erases_fc` — success path issues erase ✓
    - `finalize_upload_fails_does_not_erase` — upload failure skips erase ✓
    - `finalize_keep_fc_copy_true_skips_erase` — retention flag skips erase ✓
  - `cargo test -p roz-mavlink --test send_log_erase` → 1/1 pass (wire-level LOG_ERASE msg 121 assertion)

### 9. Mock transport + ULG fixture (SC7)
expected: |
  Check-in at `crates/roz-mavlink/tests/fixtures/px4_sample_session.ulg` — valid PX4 ULG magic bytes (`55 4C 6F 67 01 12 35`). `MockLogTransport` replays FC-side frames from fixture in response to protocol messages. Usable by protocol-level unit tests AND worker integration tests. Phase 27 SITL swap point documented inline.
result: pass
evidence: |
  `cargo test -p roz-mavlink --test mock_log_transport_unit` → 9/9 pass, including:
  - `fixture_file_is_present_and_valid_ulog_magic`
  - `fixture_digest_matches_pinned_constant` (SHA-256: ef8e9d125b1494d326ffa97476fd3ddd59df4753634b8ef81f946882dbc044ef)
  - `drop_offset_suppresses_first_delivery_then_serves_on_retry` (gap-retry harness)
  - `drive_once_on_log_request_list_returns_single_log_entry`

### 10. PX4 Flight Review human UAT (SC4) — DEFERRED
expected: |
  Download a `.ulg` artifact via `roz session export --bundle` (or direct `ArtifactService::DownloadArtifact`), upload to `flightreview.px4.io`, confirm all panels render without errors.
result: skipped
reason: |
  Deferred to human UAT checklist, same pattern as Phase 26.1 Foxglove Studio UAT. Requires Flight Review upload (external service). The synthetic fixture used in Plan 03 is a valid PX4 ULG header but is not a fully-populated flight session; a real SITL-produced `.ulg` (Phase 27) is the appropriate Flight Review input. No blocker — structural validity is confirmed; rendering is a downstream consumer concern.

## Summary

total: 10
passed: 9
issues: 0
pending: 0
skipped: 1 (SC4 Flight Review human UAT — deferred per Phase 26.1 precedent)

## Gaps

[none]
