# 26.7-03 — Server ArtifactServiceImpl + router wiring

## Outcome
Server-side ArtifactService landed end-to-end. UploadArtifact, DownloadArtifact, and ListSessionArtifacts are all implemented and wired into the multiplexed tonic Routes chain alongside the existing ObservabilityService. `ROZ_ARTIFACT_DIR` is canonicalized at boot; `AppState` carries `artifact_dir: PathBuf`.

## Key files
- `crates/roz-server/src/grpc/artifacts.rs` — 327 lines. `ArtifactServiceImpl` struct + `impl ArtifactService`. Exports `ArtifactServiceImpl`, `UPLOAD_CHUNK_SIZE = 1 MiB`.
- `crates/roz-server/src/grpc/mod.rs` — `pub mod artifacts;` added in partial-merge commit.
- `crates/roz-server/src/observability/mod.rs` — `ENV_ARTIFACT_DIR = "ROZ_ARTIFACT_DIR"` + `DEFAULT_ARTIFACT_DIR = "/var/lib/roz/artifacts"`.
- `crates/roz-server/src/state.rs` — `AppState::artifact_dir: std::path::PathBuf` field.
- `crates/roz-server/src/main.rs` — artifact_dir boot block (parallels mcap_dir), `ArtifactServiceServer::new` wired into the Routes chain, test_state/test_state_with_nats helpers grow the field.
- `crates/roz-server/src/test_support.rs` — `build_test_app_state_with_key_provider` grows the field.

## Module path for Plan 09
`roz_server::grpc::artifacts::ArtifactServiceImpl::new(pool, artifact_dir)`

## Status-code mapping

| Validation | Code |
|---|---|
| empty stream | `INVALID_ARGUMENT` |
| first frame not metadata | `INVALID_ARGUMENT` |
| session_id not UUID | `INVALID_ARGUMENT` |
| artifact_type == mcap/garbage | `INVALID_ARGUMENT` (D-03) |
| digest_sha256 len != 32 | `INVALID_ARGUMENT` |
| chunk exceeds declared size | `INVALID_ARGUMENT` |
| size mismatch on close | `INVALID_ARGUMENT` |
| digest mismatch on close | `INVALID_ARGUMENT` |
| artifact_id not UUID | `INVALID_ARGUMENT` |
| not found / cross-tenant (fetch) | `NOT_FOUND` |
| row.tenant_id != caller (belt-and-braces) | `PERMISSION_DENIED` |
| path outside ROZ_ARTIFACT_DIR | `INTERNAL` |
| DB INSERT failed | `INTERNAL` |

## Deviations
- Cargo.lock gained figment test-feature transitive deps (`parking_lot`, `tempfile`) via the Wave 1 worktree merge cascade; no action required.
- Stalled executor agent landed the RED constants + test commits before timing out; those were kept via partial merge then the GREEN path was written inline.

## Verification
- `cargo build -p roz-server` — green
- `cargo clippy -p roz-server -- -D warnings` — green
- Unit tests (extension_for, content_type_is_allowed_this_phase, UPLOAD_CHUNK_SIZE) pass via full-impl `super::*` import replacing the RED `super::super::artifacts::*`.
