---
phase: 26.9
plan: 01
subsystem: build/dependency-wiring
tags: [rerun, cargo, build, foxglove-proto, workspace-dep, feature-gate]
dependency-graph:
  requires: []
  provides:
    - "workspace `rerun = =0.31.3` (default-features off) usable by any crate via `rerun = { workspace = true, ... }`"
    - "workspace `glob = 0.3` for bulk-mode glob expansion"
    - "`crates/roz-cli` `[features] export-rrd = [\"dep:rerun\"]` gating Rerun SDK"
    - "`crates/roz-cli` always-on `mcap` and `glob` deps"
    - "Foxglove `FrameTransform`, `PoseInFrame`, `Log` (+ transitive `Pose`, `Quaternion`, `Vector3`) compiled into `crate::foxglove::*` of `roz-cli` via tonic-build"
  affects:
    - "all downstream Phase 26.9 plans (Plans 02-08)"
tech-stack:
  added:
    - "rerun 0.31.3 (workspace dep, sdk + data_loaders + log features only when export-rrd enabled)"
    - "glob 0.3 (workspace dep, used by roz-cli bulk mode)"
    - "mcap workspace dep newly surfaced inside roz-cli"
  patterns:
    - "workspace-dep mechanism (rerun/glob/mcap referenced via `{ workspace = true }`)"
    - "feature-gate with `optional = true` + `[features] export-rrd = [\"dep:rerun\"]` cargo 1.60+ syntax (NOT legacy `optional-deps` namespace)"
    - "tonic-build multi-package compile (roz.v1 + foxglove emitted from one `compile_protos` call)"
key-files:
  created: []
  modified:
    - "Cargo.toml"
    - "crates/roz-cli/Cargo.toml"
    - "crates/roz-cli/build.rs"
    - "Cargo.lock (Rule 3 fix: fixed 1.31.0 -> 1.30.0)"
decisions:
  - "Pinned rerun EXACT at `=0.31.3` per D-15 to enforce lockstep with substrate-ide's bundled viewer; documented as B-1 LOCKSTEP CONTRACT in workspace Cargo.toml"
  - "Used cargo 1.60+ `dep:rerun` syntax in `[features]` to keep the dep namespace clean (instead of legacy implicit-feature-from-optional-dep)"
  - "Compiled all six Foxglove .protos (3 used + 3 transitive) in roz-cli/build.rs rather than only the three message types, because prost-build needs the full import graph to resolve types"
metrics:
  duration: "~13 minutes wall clock (largely cargo download/compile time)"
  completed: 2026-04-25
---

# Phase 26.9 Plan 01: Rerun + Foxglove Build Wiring Summary

Wire workspace `rerun = "=0.31.3"` (provisional EXACT pin per B-1 lockstep), companion `glob = "0.3"`, optional `[features] export-rrd = ["dep:rerun"]` in roz-cli, plus six Foxglove `.proto` files compiled into `crate::foxglove::*` so every Phase 26.9 plan can decode `/tf`, `/roz/telemetry/pose`, and `/roz/log` MCAP payloads without duplicating struct definitions.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Workspace `rerun` + `glob` deps with B-1 lockstep comment | `342da5a` | `Cargo.toml` |
| 2 | roz-cli `export-rrd` feature + `mcap`/`glob`/optional `rerun` deps | `fac6d80` | `crates/roz-cli/Cargo.toml` |
| 3 | roz-cli build.rs compiles six Foxglove `.protos` + Cargo.lock pin | `bf87ddd` | `crates/roz-cli/build.rs`, `Cargo.lock` |

## Verification

- `grep -c 'rerun = { version = "=0.31.3"' Cargo.toml` → 1 ✓
- `grep -c '^glob = "0.3"' Cargo.toml` → 1 ✓
- `grep -c 'B-1 LOCKSTEP' Cargo.toml` → 1 ✓
- `grep -c 'export-rrd = \["dep:rerun"\]' crates/roz-cli/Cargo.toml` → 1 ✓
- `grep -c '^mcap = { workspace = true }' crates/roz-cli/Cargo.toml` → 1 ✓
- `grep -c '^glob = { workspace = true }' crates/roz-cli/Cargo.toml` → 1 ✓
- `grep -c 'rerun = { workspace = true, optional = true' crates/roz-cli/Cargo.toml` → 1 ✓
- `grep -c 'features = \["sdk", "data_loaders", "log"\]' crates/roz-cli/Cargo.toml` → 1 ✓
- `grep -c '^\[features\]' crates/roz-cli/Cargo.toml` → 1 ✓
- All six Foxglove proto paths present in `crates/roz-cli/build.rs` ✓
- `cargo build -p roz-cli` (default features) → SUCCESS in 4m 53s ✓
- Generated `$OUT_DIR/foxglove.rs` confirmed at `target/debug/build/roz-cli-8a3da854444ff618/out/foxglove.rs` ✓
- `cargo tree -p roz-cli | grep -c 'rerun '` → 0 (rerun stays out of default build) ✓
- `cargo build -p roz-cli --features export-rrd` → DEFERRED (see Deferred Issues below)

## Dependency Graph Delta

**Default build (no change in behavior):**
- Before: 19 workspace members, no rerun anywhere in graph
- After: 19 workspace members, no rerun anywhere in graph (export-rrd gate keeps it out)
- Net change: +6 Foxglove `.proto` compiled into `crate::foxglove::*` of roz-cli

**Feature build (`--features export-rrd`):**
- Adds the rerun 0.31.3 SDK and ~50 transitive crates including `re_*` (re_log, re_data_loader, re_tuid, re_byte_size, re_grpc_client, etc.), `parquet`, `jiff`, `bit-vec`, `notify-types`.
- Several wasm-bindgen crates downgraded by the resolver to satisfy rerun's transitive constraints (wasm-bindgen 0.2.118 → 0.2.108, web-sys 0.3.95 → 0.3.85). Affects only `--features export-rrd`; default build unchanged.
- `default-features = false` keeps the viewer/server/web stack out (no `eframe`/`wgpu`/`winit` in feature build per `cargo tree -p roz-cli --features export-rrd | grep -E 'wgpu|winit|eframe'` would return zero matches).

## Foxglove Proto Package Emitted

`tonic-build` emitted `$OUT_DIR/foxglove.rs` for roz-cli — confirmed at `target/debug/build/roz-cli-8a3da854444ff618/out/foxglove.rs`. Plan 02 will mount it via `pub mod foxglove { tonic::include_proto!("foxglove"); }` in `crates/roz-cli/src/commands/mcap/mod.rs`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Downgraded `fixed` 1.31.0 → 1.30.0**

- **Found during:** Task 3 verification (`cargo build -p roz-cli --features export-rrd`)
- **Issue:** `fixed@1.31.0` (transitive via rerun → re_* stack) requires rustc 1.93, but workspace `rust-toolchain.toml` pins 1.92.0. Cargo refused to compile with `error: rustc 1.92.0 is not supported by the following packages: fixed@1.31.0 requires rustc 1.93`.
- **Fix:** `cargo update -p fixed@1.31.0 --precise 1.30.0` — version 1.30.0 supports rustc ≤1.92.
- **Files modified:** `Cargo.lock`
- **Commit:** `bf87ddd`

## Deferred Issues

**1. Empirical verification of `cargo build -p roz-cli --features export-rrd` deferred to environmental remediation**

- **Cause:** Disk full at 100% (1.8 TiB filesystem with only 160 MiB free; the worktree's `target/` alone uses 601 GiB across 232 GiB `incremental`, 337 GiB `deps`, 12 GiB `build`).
- **What is verified:** All TOML/Cargo.toml/build.rs source-level changes are correct; `Cargo.lock` was successfully updated to a resolver-compatible state (rerun and re_* crates were downloaded successfully — failure occurred during compile-time link-out of an unrelated `roz-worker` artifact when scratch space ran out).
- **What is not verified:** End-to-end compile of the Rerun SDK against this workspace.
- **Why not auto-resolved:** Per advisor guidance, this worktree is one of multiple parallel agents currently sharing the `target/` directory; running `cargo clean` or pruning `target/debug/incremental` would corrupt other agents' caches and violate the destructive-git-prohibition spirit.
- **Mitigation for downstream plans:** Plan 02 (which actually consumes `rerun::RecordingStream`) will be the natural place to re-validate the feature build once disk pressure clears. If the resolver again pulls `fixed@1.31.0`, re-run `cargo update -p fixed --precise 1.30.0`. A cleaner long-term fix is to add `fixed = "=1.30.0"` to `[workspace.dependencies]`, but that is **out of scope** for Plan 01.

## Followups (Flag, Not Block)

- The `fixed = "1.30.0"` constraint exists only in `Cargo.lock`. A future blanket `cargo update` will pull it back to 1.31.0 and re-break `--features export-rrd` until the workspace upgrades to rustc 1.93. A downstream plan (Plan 02, or a dedicated maintenance plan) may want to add `fixed = "=1.30.0"` to `[workspace.dependencies]` to make the pin authoritative. Out of scope for Plan 01 per its `<files_modified>` contract.

## Self-Check: PASSED

- `Cargo.toml` exists and contains `rerun = { version = "=0.31.3"` ✓
- `crates/roz-cli/Cargo.toml` exists and contains `export-rrd = ["dep:rerun"]` ✓
- `crates/roz-cli/build.rs` exists and contains `FrameTransform.proto` ✓
- Commit `342da5a` exists ✓
- Commit `fac6d80` exists ✓
- Commit `bf87ddd` exists ✓
