---
phase: "26.11"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/roz-worker/src/physical_runtime.rs
  - crates/roz-worker/src/task_invocation_harness.rs
  - crates/roz-worker/src/lib.rs
  - crates/roz-worker/src/main.rs
  - crates/roz-worker/src/tools/flight_command.rs
  - crates/roz-worker/src/tools/mod.rs
  - crates/roz-worker/tests/manipulator_harness_vertical.rs
  - crates/roz-worker/tests/flight_command_tool_routing.rs
autonomous: true
requirements: [TR-02]
tags: [reference-manipulator, manipulator, flight-command, vertical-test, fake-backend, tool-routing]

must_haves:
  truths:
    - "Golden reference manipulator test drives a real `TaskInvocation` in `ExecutionMode::OodaReAct` through the worker task/agent/tool path; it does not call `spawn_physical_runtime` or individual physical tools directly from the test."
    - "Golden reference manipulator test proves fake backend state changed; an `Ok` return or command-sent assertion alone is not accepted."
    - "Golden reference manipulator positive and negative variants assert session/tool telemetry evidence from the same worker task path, including `ToolCallFinished` or `SafetyIntervention` evidence."
    - "Golden reference manipulator negative variants cover missing backend/sink structured failure and restrictive safety preventing unsafe output through the task path."
    - "This plan validates Roz's manipulator-class harness abstraction; it does not claim upstream hardware behavior or protocol compatibility."
    - "`flight_command` is one agent-visible tool with `{command: ...}` variant arg, is registered in `ToolDispatcher`, retrieves a concrete extension handle, and calls `DiscreteCommandSink<FlightCommand>`."
    - "Missing flight sink produces `ToolResult::error`, and denied MAVLink ACK produces a failed tool result visible to the agent."
  artifacts:
    - path: "crates/roz-worker/src/physical_runtime.rs"
      provides: "small production seam shared by main.rs and vertical tests"
      contains: "spawn_physical_runtime"
    - path: "crates/roz-worker/src/task_invocation_harness.rs"
      provides: "deterministic worker TaskInvocation/OodaReAct harness for tests"
      contains: "run_worker_task_invocation_for_tests"
    - path: "crates/roz-worker/src/tools/flight_command.rs"
      provides: "minimal Phase 27-aligned flight_command tool route"
      contains: "FlightCommandTool"
    - path: "crates/roz-worker/tests/manipulator_harness_vertical.rs"
      provides: "golden reference manipulator vertical acceptance"
      contains: "manipulator_task_invocation_agent_to_worker_to_fake_backend_records_state_telemetry_and_session_evidence"
    - path: "crates/roz-worker/tests/flight_command_tool_routing.rs"
      provides: "flight_command schema/dispatch/sink outcome coverage"
      contains: "denied_ack_surfaces_failed_tool_result"
  key_links:
    - from: "crates/roz-worker/src/main.rs"
      to: "crates/roz-worker/src/physical_runtime.rs"
      via: "shared runtime assembly helper"
      pattern: "spawn_physical_runtime"
    - from: "crates/roz-worker/tests/manipulator_harness_vertical.rs"
      to: "crates/roz-worker/src/task_invocation_harness.rs"
      via: "TaskInvocation/OodaReAct task execution harness"
      pattern: "run_worker_task_invocation_for_tests"
    - from: "crates/roz-worker/src/tools/flight_command.rs"
      to: "crates/roz-agent/src/dispatch/mod.rs::Extensions"
      via: "concrete extension retrieval"
      pattern: "extensions.get::<FlightCommandSinkHandle>"
    - from: "crates/roz-worker/tests/manipulator_harness_vertical.rs"
      to: "crates/roz-copper/src/fake_manipulator.rs"
      via: "FakeManipulatorFactory selected by IoFactory"
      pattern: "factory_for\\(Some\\(\"reference-manipulator\"\\)\\)"
---

<objective>
Add the main robotics acceptance evidence: a golden reference manipulator vertical test and a narrow `flight_command` tool-routing seam aligned with Phase 27.

Purpose: The audit requires production-boundary tests, not isolated fake-unit checks. This plan proves manipulator state change and flight command routing through the same `ToolDispatcher`/`Extensions` pattern the agent uses.
Output: A shared physical-runtime test seam, reference manipulator vertical tests, and minimal `flight_command` tool route tests.
</objective>

<execution_context>
@/Users/krnzt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/krnzt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
@.planning/phases/26.10-physical-control-production-wiring-authoritative-embodiment-runtime-/26.10-VERIFICATION.md
@.planning/phases/27-nightly-px4-sitl-integration-ci-with-induced-nats-outage-liv/27-CONTEXT.md
@crates/roz-worker/src/main.rs
@crates/roz-worker/src/io_backends.rs
@crates/roz-worker/src/tools/promote_controller.rs
@crates/roz-worker/src/tools/controller_status.rs
@crates/roz-worker/src/tools/stop_controller.rs
@crates/roz-copper/src/fake_manipulator.rs
@crates/roz-agent/src/dispatch/mod.rs
@crates/roz-mavlink/src/flight_command.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive golden reference manipulator through TaskInvocation/OodaReAct worker execution</name>
  <files>crates/roz-worker/src/physical_runtime.rs, crates/roz-worker/src/task_invocation_harness.rs, crates/roz-worker/src/lib.rs, crates/roz-worker/src/main.rs, crates/roz-worker/tests/manipulator_harness_vertical.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - .planning/phases/26.10-physical-control-production-wiring-authoritative-embodiment-runtime-/26.10-VERIFICATION.md
    - crates/roz-worker/src/main.rs
    - crates/roz-worker/src/io_backends.rs
    - crates/roz-worker/src/tools/promote_controller.rs
    - crates/roz-worker/src/tools/controller_status.rs
    - crates/roz-worker/src/tools/stop_controller.rs
    - crates/roz-worker/src/dispatch.rs
    - crates/roz-worker/src/model_factory.rs
    - crates/roz-worker/tests/manipulator_dispatch_path.rs
    - crates/roz-agent/tests/e2e_wave_arm.rs
    - crates/roz-copper/src/fake_manipulator.rs
    - crates/roz-copper/src/handle.rs
  </read_first>
  <behavior>
    - Test `manipulator_task_invocation_agent_to_worker_to_fake_backend_records_state_telemetry_and_session_evidence`: build a `TaskInvocation` with `ExecutionMode::OodaReAct`, authoritative reference manipulator `embodiment_runtime`, and `control_interface_manifest`; run it through a deterministic worker task harness with a scripted model that emits `promote_controller`, `controller_status`, and `stop_controller`; wait on fake-backend state with a bounded timeout; assert at least one joint position or velocity changes from zero; assert telemetry/session evidence includes the task id and at least one `ToolCallFinished` event.
    - Test `missing_manipulator_backend_returns_structured_failure_from_task_invocation`: run the same harness with an unknown family id and assert the task result/status contains `no IoFactory for embodiment_family` without calling `spawn_physical_runtime` directly from the test.
    - Test `restrictive_safety_prevents_unsafe_manipulator_output_from_task_invocation`: run the same command under restrictive policy through the task harness and assert actuator-observed values never exceed the configured cap while the permissive policy variant does move and emits normal tool/session evidence.
  </behavior>
  <action>
    Create `crates/roz-worker/src/physical_runtime.rs` and export it from `crates/roz-worker/src/lib.rs`. Extract only the physical runtime assembly already present in `crates/roz-worker/src/main.rs` OodaReAct handling:

    - Resolve `factory_for(runtime.model.embodiment_family.as_deref())`.
    - Call the selected `IoFactory::build(&runtime, &control_manifest)`.
    - Spawn `CopperHandle::spawn_with_policy_and_io(max_velocity, actuator, Some(sensor), hot_policy, backpressure, latch_persist_tx)`.
    - Register `promote_controller`, `controller_status`, and `stop_controller` with `ToolCategory::Physical`.
    - Build `ToolContext.extensions` with exactly the same values the worker path uses: `mpsc::Sender<ControllerCommand>`, `Arc<ArcSwap<ControllerState>>`, `ControlInterfaceManifest`, and `EmbodimentRuntime`.

    Name the helper `spawn_physical_runtime` and return a `PhysicalRuntimeHandle` containing `copper: CopperHandle`, `dispatcher: ToolDispatcher`, `context: ToolContext`, and any fake-backend observation handle needed by tests. Keep the helper production-usable; do not put the implementation behind `#[cfg(test)]`. Use `#[cfg(feature = "test-fixtures")]` only for fake manipulator observation details that cannot exist in production.

    Update `crates/roz-worker/src/main.rs` to call `spawn_physical_runtime` instead of duplicating this assembly. Behavior must stay unchanged for production worker execution.

    Create `crates/roz-worker/src/task_invocation_harness.rs` behind `#[cfg(feature = "test-fixtures")]` and export it from `crates/roz-worker/src/lib.rs` only for that feature. This harness must be a thin test adapter over the worker task path, not a new physical shortcut:

    - Accept a real `roz_nats::dispatch::TaskInvocation`, a deterministic `Box<dyn roz_agent::model::Model>`, worker config/policy inputs, and a session-event sink.
    - Reuse `roz_worker::dispatch::build_turn_input`, `build_prompt_state`, `build_runtime_shell_input`, `effective_cognition_mode`, and the same `ToolDispatcher` registration set used by `main.rs` for OodaReAct tasks.
    - Invoke `AgentLoop::new(...).with_extensions(...)` or the equivalent `SessionRuntime` streaming worker path with the supplied model so the scripted tool calls go through `ToolDispatcher::dispatch`.
    - Call `spawn_physical_runtime` only inside the worker harness/production assembly, never directly from `manipulator_harness_vertical.rs`.
    - Return a `WorkerTaskHarnessResult` containing terminal status, fake manipulator observation handle, collected session events, collected tool telemetry/evidence, and the final controller state snapshot.

    Add `crates/roz-worker/tests/manipulator_harness_vertical.rs` gated with `#![cfg(feature = "test-fixtures")]`. Use `roz_core::embodiment::test_fixtures::manipulator_runtime(2, 1.0, 3.14)` or the reference manipulator fixture if the helper already exists. Use a deterministic `MockModel` response sequence patterned after `crates/roz-agent/tests/e2e_wave_arm.rs`, but emit the worker physical tool calls required for this test. Use bounded waits with `tokio::time::timeout(Duration::from_secs(5), ...)`; do not use a fixed sleep as the only synchronization.

    The golden test must assert all of these exact evidence points:
    - `TaskInvocation::new(...)` is constructed with `ExecutionMode::OodaReAct`, `embodiment_runtime: Some(...)`, and `control_interface_manifest: Some(...)`;
    - the harness-visible dispatcher `tool_names()` contains `promote_controller`, `controller_status`, and `stop_controller`;
    - `controller_status` reports running after promotion and stopped after stop through `ToolDispatcher::dispatch`;
    - fake manipulator sensor state changes: at least one `joint_positions[i]` or `joint_velocities[i]` differs from its pre-command value by more than `1e-6`;
    - session evidence is recorded as at least one `ToolCallFinished` or `SafetyIntervention` event through the same event emission path already used by worker/session tests;
    - telemetry evidence includes the `TaskInvocation.task_id` and at least one tool-call lifecycle event for the promoted controller path.
  </action>
  <verify>
    <automated>cargo test -p roz-worker --features test-fixtures --test manipulator_harness_vertical -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "pub mod physical_runtime" crates/roz-worker/src/lib.rs` returns one line.
    - `grep -F "pub mod task_invocation_harness" crates/roz-worker/src/lib.rs` returns one line behind the `test-fixtures` feature.
    - `grep -F "pub fn spawn_physical_runtime" crates/roz-worker/src/physical_runtime.rs` returns one line.
    - `grep -F "run_worker_task_invocation_for_tests" crates/roz-worker/src/task_invocation_harness.rs` returns one line.
    - `grep -F "AgentLoop::new" crates/roz-worker/src/task_invocation_harness.rs` or `grep -F "SessionRuntime::new" crates/roz-worker/src/task_invocation_harness.rs` returns at least one line.
    - `grep -F "factory_for" crates/roz-worker/src/physical_runtime.rs` returns at least one line.
    - `grep -F "CopperHandle::spawn_with_policy_and_io" crates/roz-worker/src/physical_runtime.rs` returns at least one line.
    - `grep -F "manipulator_task_invocation_agent_to_worker_to_fake_backend_records_state_telemetry_and_session_evidence" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns one line.
    - `grep -F "TaskInvocation::new" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns at least one line.
    - `grep -F "ExecutionMode::OodaReAct" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns at least one line.
    - `grep -F "run_worker_task_invocation_for_tests" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns at least one line.
    - `grep -F "spawn_physical_runtime" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns no lines.
    - `grep -E "joint_(positions|velocities).*1e-6|1e-6.*joint_(positions|velocities)" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns at least one line.
    - `grep -E "ToolCallFinished|SafetyIntervention" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns at least one line.
    - `grep -F "tokio::time::timeout" crates/roz-worker/tests/manipulator_harness_vertical.rs` returns at least one line.
    - `cargo test -p roz-worker --features test-fixtures --test manipulator_harness_vertical -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Manipulator harness acceptance proves state change, telemetry evidence, session evidence, and failure/safety variants through a real TaskInvocation/OodaReAct worker path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add minimal `flight_command` tool routing through Extensions and DiscreteCommandSink</name>
  <files>crates/roz-worker/src/tools/flight_command.rs, crates/roz-worker/src/tools/mod.rs, crates/roz-worker/tests/flight_command_tool_routing.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - .planning/phases/27-nightly-px4-sitl-integration-ci-with-induced-nats-outage-liv/27-CONTEXT.md
    - crates/roz-agent/src/dispatch/mod.rs
    - crates/roz-core/src/tools.rs
    - crates/roz-copper/src/io.rs
    - crates/roz-mavlink/src/flight_command.rs
    - crates/roz-mavlink/src/backend.rs
    - crates/roz-worker/src/tools/mod.rs
  </read_first>
  <behavior>
    - Test `schema_exposes_one_flight_command_tool_with_variant_arg`: schema name is exactly `flight_command`; required params include `command`.
    - Test `missing_sink_returns_unavailable_tool_result`: register `FlightCommandTool` in `ToolDispatcher`, dispatch a `ToolCall` named `flight_command` without the sink extension, and assert `ToolResult::error` with message containing `flight_command unavailable`.
    - Test `arm_command_routes_to_discrete_sink`: register `FlightCommandTool` in `ToolDispatcher`, insert the concrete extension handle, dispatch a `ToolCall` named `flight_command` with schema-shaped `{ "command": "arm" }`, assert `send_command(FlightCommand::Arm(_))` exactly once and success JSON containing `"mav_result":"accepted"`.
    - Test `denied_ack_surfaces_failed_tool_result`: register `FlightCommandTool` in `ToolDispatcher`, dispatch by tool name with a fake sink returning `MavResult::Denied`, and assert `ToolResult::error` or success=false JSON with error containing `denied by MAVLink ACK`.
  </behavior>
  <action>
    Create `crates/roz-worker/src/tools/flight_command.rs` with a minimal Phase 27-aligned tool. Do not implement SITL, IoFactory, readiness propagation, or full Phase 27 worker boot.

    Required concrete API:
    - `pub struct FlightCommandTool;`
    - `pub struct FlightCommandSinkHandle(...)` as a concrete extension key. This wrapper may hold `Arc<dyn DiscreteCommandSink<FlightCommand, Response = FlightCommandResponse, Error = MavlinkDispatchError> + Send + Sync>` or an enum/newtype, but the same concrete `FlightCommandSinkHandle` type must be inserted into and retrieved from `Extensions`.
    - `FlightCommandTool` implements `TypedToolExecutor` with `name() == "flight_command"`.
    - Input schema includes `command: String` plus optional fields needed by existing variants: `altitude_m`, `mode`, `latitude_deg`, `longitude_deg`, `relative_altitude_m`.
    - Supported `command` string values: `arm`, `disarm`, `takeoff`, `land`, `rtl`, `set_mode`, `goto`.
    - Missing extension returns `Ok(ToolResult::error("flight_command unavailable: MAVLink sink missing".to_string()))`.
    - If sink response `result != MavResult::Accepted`, return visible failure with message `flight_command denied by MAVLink ACK: {result:?} {error}`.

    Export `pub mod flight_command;` from `crates/roz-worker/src/tools/mod.rs`.

    Add `crates/roz-worker/tests/flight_command_tool_routing.rs`. Use a fake sink type in the test that records received `FlightCommand` values in `Arc<Mutex<Vec<FlightCommand>>>` and returns configurable `FlightCommandResponse`. Insert the sink as `FlightCommandSinkHandle` into `ToolContext.extensions`; do not insert a dyn trait directly into `Extensions`.

    Every test must drive the public dispatcher path:
    - create `let mut dispatcher = ToolDispatcher::new(Duration::from_secs(5));`;
    - call `dispatcher.register_with_category(Box::new(FlightCommandTool), ToolCategory::Physical);`;
    - assert `dispatcher.schemas()` includes `flight_command` and required `command`;
    - create `roz_core::tools::ToolCall { id, tool: "flight_command".into(), params }`;
    - call `dispatcher.dispatch(&call, &ctx).await`.

    Do not call `TypedToolExecutor::execute(&FlightCommandTool, ...)` directly in these tests; that bypasses the agent-visible tool routing contract this plan is closing.
  </action>
  <verify>
    <automated>cargo test -p roz-worker --test flight_command_tool_routing -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "pub mod flight_command" crates/roz-worker/src/tools/mod.rs` returns one line.
    - `grep -F "pub struct FlightCommandTool" crates/roz-worker/src/tools/flight_command.rs` returns one line.
    - `grep -F "pub struct FlightCommandSinkHandle" crates/roz-worker/src/tools/flight_command.rs` returns one line.
    - `grep -F "extensions.get::<FlightCommandSinkHandle>" crates/roz-worker/src/tools/flight_command.rs` returns one line.
    - `grep -F "flight_command unavailable: MAVLink sink missing" crates/roz-worker/src/tools/flight_command.rs` returns one line.
    - `grep -F "denied by MAVLink ACK" crates/roz-worker/src/tools/flight_command.rs` returns one line.
    - `grep -F "dispatcher.register_with_category(Box::new(FlightCommandTool)" crates/roz-worker/tests/flight_command_tool_routing.rs` returns at least one line.
    - `grep -F 'tool: "flight_command".into()' crates/roz-worker/tests/flight_command_tool_routing.rs` returns at least one line.
    - `grep -F "dispatcher.dispatch(&call, &ctx).await" crates/roz-worker/tests/flight_command_tool_routing.rs` returns at least one line.
    - `grep -F "TypedToolExecutor::execute" crates/roz-worker/tests/flight_command_tool_routing.rs` returns no lines.
    - `cargo test -p roz-worker --test flight_command_tool_routing -- --nocapture` exits 0 and includes `denied_ack_surfaces_failed_tool_result`.
  </acceptance_criteria>
  <done>`flight_command` is testable through `ToolDispatcher` registration/dispatch and concrete extension map without broadening into Phase 27 SITL.</done>
</task>

</tasks>

<verification>
- `cargo fmt --check`
- `cargo test -p roz-worker --features test-fixtures --test manipulator_harness_vertical -- --nocapture`
- `cargo test -p roz-worker --test flight_command_tool_routing -- --nocapture`
- `cargo test -p roz-worker --features test-fixtures --test manipulator_dispatch_path`
</verification>

<success_criteria>
- reference manipulator vertical test proves state transition and session evidence.
- Flight command route proves schema -> dispatcher -> `Extensions` -> `DiscreteCommandSink<FlightCommand>` for success, missing sink, and denied ACK.
- No Phase 27 SITL or real-FCU implementation is pulled into 26.11.
</success_criteria>

<output>
After completion, create `.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-02-SUMMARY.md`.
</output>
