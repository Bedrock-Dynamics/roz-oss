---
phase: "26.11"
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - crates/roz-server/Cargo.toml
  - crates/roz-server/tests/safety_policy_physical_vertical.rs
autonomous: true
requirements: [TR-03]
tags: [safety, vertical-test, signed-nats, reference-manipulator, manipulator, metadata-index]

must_haves:
  truths:
    - "Safety vertical drives server API policy write -> signed NATS push -> worker verify/apply -> policy cache/hot policy -> Copper/fake manipulator-class manipulator clamp or reject."
    - "Unsafe physical values do not reach the actuator sink under restrictive policy."
    - "Permissive policy allows the same command that restrictive policy clamps or rejects."
    - "SafetyIntervention or SafetyViolation evidence is archived and queryable through session metadata/index rows."
  artifacts:
    - path: "crates/roz-server/tests/safety_policy_physical_vertical.rs"
      provides: "full-chain safety policy physical vertical test"
      contains: "restrictive_policy_blocks_unsafe_manipulator_output_and_indexes_evidence"
    - path: "crates/roz-server/Cargo.toml"
      provides: "required-features entry for safety vertical test"
      contains: "safety_policy_physical_vertical"
  key_links:
    - from: "crates/roz-server/tests/safety_policy_physical_vertical.rs"
      to: "crates/roz-server/tests/phase24_policy_crud_live.rs"
      via: "HTTP policy CRUD + signed fanout harness"
      pattern: "POST /v1/safety-policies"
    - from: "crates/roz-server/tests/safety_policy_physical_vertical.rs"
      to: "crates/roz-copper/src/fake_manipulator.rs"
      via: "restrictive policy applied before fake actuator observation"
      pattern: "fake_manipulator"
    - from: "crates/roz-server/tests/safety_policy_physical_vertical.rs"
      to: "crates/roz-server/tests/session_metadata_fleet_query.rs"
      via: "metadata index query after MCAP evidence"
      pattern: "intervention_count"
---

<objective>
Add a safety-policy vertical acceptance test that proves the policy chain reaches physical output and archived evidence.

Purpose: Existing Phase 24 tests prove server fanout and copper clamp as separate slices. This plan joins them so a restrictive policy demonstrably prevents unsafe output from Roz's deterministic fake manipulator backend and leaves queryable evidence.
Output: One ignored, nightly-routed server integration test gated behind `test-helpers`.
</objective>

<execution_context>
@/Users/krnzt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/krnzt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
@crates/roz-server/tests/phase24_policy_crud_live.rs
@crates/roz-worker/tests/phase24_policy_pushstack.rs
@crates/roz-copper/tests/phase24_policy_tick_clamp.rs
@crates/roz-server/tests/session_metadata_fleet_query.rs
@crates/roz-worker/src/policy_enforcement.rs
@crates/roz-worker/src/policy_cache.rs
@crates/roz-copper/src/fake_manipulator.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create server-to-worker-to-Copper safety vertical test</name>
  <files>crates/roz-server/Cargo.toml, crates/roz-server/tests/safety_policy_physical_vertical.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - crates/roz-server/Cargo.toml
    - crates/roz-worker/Cargo.toml
    - crates/roz-server/tests/phase24_policy_crud_live.rs
    - crates/roz-worker/tests/phase24_policy_pushstack.rs
    - crates/roz-copper/tests/phase24_policy_tick_clamp.rs
    - crates/roz-server/tests/session_metadata_fleet_query.rs
    - crates/roz-worker/src/policy_enforcement.rs
    - crates/roz-worker/src/policy_cache.rs
    - crates/roz-copper/src/fake_manipulator.rs
    - crates/roz-worker/src/physical_runtime.rs
  </read_first>
  <behavior>
    - Test `restrictive_policy_blocks_unsafe_manipulator_output_and_indexes_evidence`: POST restrictive policy, observe signed policy push, worker verifies/applies it, execute unsafe reference manipulator command, assert actuator-observed output is clamped or rejected and metadata has at least one intervention.
    - Test `permissive_policy_allows_same_manipulator_command`: same command under permissive policy produces fake backend state delta > `1e-6` and no intervention row for that session.
  </behavior>
  <action>
    First make the server test feature path executable with the fake manipulator-class worker fixture. Update `crates/roz-server/Cargo.toml` so the existing `test-helpers` feature includes `roz-worker/test-fixtures`:

    ```toml
    test-helpers = ["roz-agent/test-helpers", "roz-worker/test-fixtures"]
    ```

    This is the narrower reliable shape for this plan: the physical fake manipulator-class manipulator evidence remains in the server vertical test, and the exact Cargo command below turns on the worker fixture feature through the server test feature path. Do not rely on default server tests seeing `roz-worker` fixtures implicitly.

    Add a `[[test]]` entry to `crates/roz-server/Cargo.toml`:

    ```toml
    [[test]]
    name = "safety_policy_physical_vertical"
    path = "tests/safety_policy_physical_vertical.rs"
    required-features = ["test-helpers"]
    ```

    Create `crates/roz-server/tests/safety_policy_physical_vertical.rs` with `#![cfg(feature = "test-helpers")]` and one ignored test:

    ```rust
    #[tokio::test(flavor = "multi_thread", worker_threads = 4)]
    #[ignore = "requires testcontainers Postgres + NATS + --features test-helpers"]
    async fn restrictive_policy_blocks_unsafe_manipulator_output_and_indexes_evidence() { ... }
    ```

    The test must combine existing harness patterns instead of inventing new setup:

    - Use Postgres/NATS setup and `POST /v1/safety-policies` pattern from `phase24_policy_crud_live.rs`.
    - Subscribe to `Subjects::policy(worker_id)` before POST and verify the received payload with `WorkerSigningContext::verify_inbound_worker`.
    - Apply the row with `apply_policy_push(&row, &PolicyCache, &HotPolicy, &HotCopperPolicy)` so both `PolicyCache` and `HotCopperPolicy` are updated.
    - Spawn the deterministic fake manipulator through `spawn_physical_runtime` from Plan 02 using the hot copper policy that was updated from the signed push.
    - Use a controller/test command that would produce a raw value above the restrictive limit, for example raw velocity `0.8` with policy max `0.5`.
    - Assert the fake actuator/sensor never observes a value over `0.5 + 1e-6`.
    - Run a permissive branch with max `100.0` and assert the same command produces a fake backend state delta greater than `1e-6`.

    Evidence/index requirement:
    - Route the produced `SafetyIntervention` or `SafetyViolation` event through the production MCAP event converter used by `session_metadata_fleet_query.rs`, then finalize and call `index_session`.
    - Query `roz_session_metadata` for the session id and assert `intervention_count >= 1`.
    - Do not satisfy this by directly inserting into `roz_session_metadata`; the row must come from indexing archived session evidence.

    Synchronization must use bounded polling/timeouts on NATS message receipt, fake backend state, archive finalization, and metadata row visibility. Do not use fixed sleeps as the only wait.
  </action>
  <verify>
    <automated>cargo test -p roz-server --features test-helpers --test safety_policy_physical_vertical -- --ignored --test-threads=1 --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'roz-worker/test-fixtures' crates/roz-server/Cargo.toml` returns one line in the `test-helpers` feature.
    - `grep -F 'name = "safety_policy_physical_vertical"' crates/roz-server/Cargo.toml` returns one line.
    - `grep -F "restrictive_policy_blocks_unsafe_manipulator_output_and_indexes_evidence" crates/roz-server/tests/safety_policy_physical_vertical.rs` returns one line.
    - `grep -F "POST" crates/roz-server/tests/safety_policy_physical_vertical.rs` and `grep -F "/v1/safety-policies" crates/roz-server/tests/safety_policy_physical_vertical.rs` both return lines.
    - `grep -F "verify_inbound_worker" crates/roz-server/tests/safety_policy_physical_vertical.rs` returns at least one line.
    - `grep -F "apply_policy_push" crates/roz-server/tests/safety_policy_physical_vertical.rs` returns at least one line.
    - `grep -F "intervention_count" crates/roz-server/tests/safety_policy_physical_vertical.rs` returns at least one line.
    - `grep -F "tokio::time::timeout" crates/roz-server/tests/safety_policy_physical_vertical.rs` returns at least one line.
    - `grep -E "INSERT INTO roz_session_metadata|session_metadata::upsert" crates/roz-server/tests/safety_policy_physical_vertical.rs` returns no lines.
    - `cargo test -p roz-server --features test-helpers --test safety_policy_physical_vertical -- --ignored --test-threads=1 --nocapture` exits 0.
  </acceptance_criteria>
  <done>Safety policy vertical proves both physical prevention and queryable archived evidence.</done>
</task>

</tasks>

<verification>
- `cargo fmt --check`
- `cargo test -p roz-server --features test-helpers --test safety_policy_physical_vertical -- --ignored --test-threads=1 --nocapture`
- `cargo test -p roz-server --features test-helpers --no-run`
</verification>

<success_criteria>
- Restrictive policy blocks or clamps unsafe fake manipulator output.
- Permissive policy allows the same command.
- Evidence is archived and indexed; the test asserts metadata, not only in-memory outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-03-SUMMARY.md`.
</output>
