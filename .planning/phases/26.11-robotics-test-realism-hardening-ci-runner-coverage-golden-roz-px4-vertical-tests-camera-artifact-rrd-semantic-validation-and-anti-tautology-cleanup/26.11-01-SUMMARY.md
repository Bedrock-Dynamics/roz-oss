# Plan 01 Summary: CI Runner Coverage + MAVLink Teardown

## Completed

- Added a dedicated `robotics-feature-tests` CI job for feature-gated robotics suites:
  - `roz-server --features test-helpers`
  - `roz-cli --features export-rrd --test mcap_to_rrd`
  - `roz-worker --features test-fixtures`
  - `roz-mavlink --features test-helpers --tests`
- Added a `robotics-test-realism` nightly nextest job for ignored robotics integration suites and wired it into nightly failure reporting.
- Extended `.config/nextest.toml` allowlist with the Phase 26.11 vertical/golden test binaries.
- Replaced sync MAVLink transport `recv()`/`send()` bridging with upstream `mavlink::connect_async`, making UDP transport tasks cancellable by Tokio.
- Added `TransportHandle::shutdown()` and `MavlinkBackend::shutdown_for_tests()` for deterministic live-transport test teardown.
- Removed all `std::process::exit(0)` teardown calls from the affected MAVLink/worker tests.
- `qgc_coexistence` now runs both signed and unsigned variants in one test process.

## Verification

- `cargo check -p roz-mavlink --features test-helpers` passed.
- `rustfmt --edition 2024 --check` passed on the Wave 1 Rust files.
- `cargo test -p roz-mavlink --features test-helpers --test autopilot_kind` passed.
- `cargo test -p roz-mavlink --features test-helpers --test log_fanout` passed.
- `cargo test -p roz-mavlink --features test-helpers --test send_log_erase` passed.
- `cargo test -p roz-mavlink --features test-helpers --test qgc_coexistence` passed: 2/2 tests.
- `cargo test -p roz-worker --features test-fixtures --test mavlink_backend_null_key` passed.
- `cargo test -p roz-cli --features export-rrd --test mcap_to_rrd` passed: 1 passed, 1 expected fixture-gated ignored.
- `cargo test -p roz-worker --features test-fixtures --no-run` passed.

## Notes

- `cargo fmt --all --check` is not a valid gate for this wave right now because unrelated files outside the Wave 1 write scope have pre-existing rustfmt drift. I avoided normalizing those files to keep this plan atomic.
