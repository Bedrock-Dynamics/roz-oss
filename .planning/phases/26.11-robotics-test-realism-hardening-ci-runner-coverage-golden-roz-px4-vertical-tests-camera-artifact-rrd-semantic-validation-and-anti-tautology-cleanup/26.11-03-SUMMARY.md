# Plan 26.11-03 Summary

## Status

Implemented Plan 03 safety-policy physical vertical coverage.

## Changes

- Updated `roz-server`'s `test-helpers` feature to also enable `roz-worker/test-fixtures`.
- Added the `safety_policy_physical_vertical` test target gated behind `test-helpers`.
- Added `crates/roz-server/tests/safety_policy_physical_vertical.rs`, an ignored vertical test that:
  - POSTs restrictive and permissive policies through `/v1/safety-policies`.
  - Observes signed NATS fanout on `Subjects::policy(worker_id)`.
  - Verifies the fanout with `WorkerSigningContext::verify_inbound_worker`.
  - Applies the policy row with `apply_policy_push` into `PolicyCache`, `HotPolicy`, and `HotCopperPolicy`.
  - Spawns the deterministic fake manipulator physical runtime through `spawn_physical_runtime`.
  - Proves restrictive policy clamps unsafe actuator-observed output, while permissive policy allows the same command above the restrictive cap.
  - Routes SafetyIntervention evidence through the MCAP writer/event converter and asserts `intervention_count` from `index_session`.

## Verification

- `rustfmt --edition 2024 crates/roz-server/tests/safety_policy_physical_vertical.rs`
- `cargo test -p roz-server --features test-helpers --test safety_policy_physical_vertical --no-run`
  - Passed compile.
- `cargo test -p roz-server --features test-helpers --no-run`
  - Passed compile.
- Static acceptance greps passed, including the negative check that the test does not directly insert/upsert `roz_session_metadata`.
- `cargo test -p roz-server --features test-helpers --test safety_policy_physical_vertical -- --ignored --test-threads=1 --nocapture`
  - Failed before assertions because local Docker/testcontainers timed out creating the Postgres container: `Client(CreateContainer(RequestTimeoutError))`.
- `cargo fmt --check --package roz-server`
  - Failed on unrelated pre-existing formatting in `crates/roz-server/src/routes/task_dispatch.rs`.

## Notes

- The test text explicitly treats the fixture as Roz's reference manipulator path, not upstream reference manipulator compatibility.
- MCAP metadata evidence is not faked through direct DB writes; the test archives session events and calls `index_session`.
