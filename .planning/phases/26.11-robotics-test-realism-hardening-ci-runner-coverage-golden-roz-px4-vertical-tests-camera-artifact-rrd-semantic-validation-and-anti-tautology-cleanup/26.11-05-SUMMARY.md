# Plan 26.11-05 Summary

## Status

Implemented Plan 05 artifact and bundle vertical coverage.

## Changes

- Added `crates/roz-cli/tests/session_bundle_export_live.rs`, an ignored live CLI test that starts loopback ObservabilityService and ArtifactService gRPC handlers, runs the real `roz session export <id> --format mcap --bundle --output <path>` subprocess, and verifies a tar bundle containing `manifest.json`, MCAP rollover content, copper artifact content, sizes, and SHA-256 digests.
- Added `roz-server` as a `roz-cli` dev-dependency so the CLI integration test can reuse the generated service server types without generating a second server module.
- Fixed CLI argument handling so the global `--format` flag does not conflict with the session export `--format mcap` flag, and assigned the session export format argument a distinct clap id.
- Added `crates/roz-worker/tests/copper_artifact_finalize.rs`, ignored container-backed worker tests for one-row-per-copper-segment upload, delete-on-success cleanup, and soft-fail local retention.
- Kept the bundle test in `roz-cli`; no server-side test target was added.

## Verification

- `rustfmt --edition 2024 crates/roz-cli/src/commands/session.rs crates/roz-cli/tests/session_bundle_export_live.rs crates/roz-worker/tests/copper_artifact_finalize.rs`
- `rustfmt --edition 2024 crates/roz-cli/src/cli.rs crates/roz-cli/src/commands/session.rs`
- `cargo test -p roz-worker --test copper_artifact_finalize --no-run`
  - Passed compile.
- `cargo test -p roz-cli --test session_bundle_export_live --no-run`
  - Passed compile.
- `cargo test -p roz-cli --lib commands::session`
  - Passed: 8 passed.
- `cargo test -p roz-cli --test session_bundle_export_live -- --ignored --test-threads=1 --nocapture`
  - Passed: 1 passed.
- `cargo test -p roz-worker --test copper_artifact_finalize -- --ignored --test-threads=1 --nocapture`
  - Soft-fail retention case passed.
  - DB-backed cases failed before assertions because local Docker/testcontainers timed out creating the Postgres container: `Client(CreateContainer(RequestTimeoutError))`.

## Notes

- The live bundle test intentionally invokes the actual `roz` binary through `CARGO_BIN_EXE_roz`; it does not call bundle helpers directly.
- The copper tests exercise `finalize_copper_archive`; the two DB-backed cases remain CI-routed through their ignore reason because they require the Postgres testcontainer path to be healthy.
