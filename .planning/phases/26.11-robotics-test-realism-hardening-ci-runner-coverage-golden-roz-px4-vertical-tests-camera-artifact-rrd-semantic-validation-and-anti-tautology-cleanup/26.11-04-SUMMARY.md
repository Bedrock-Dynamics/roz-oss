# Plan 26.11-04 Summary

## Status

Implemented Plan 04 camera/RRD anti-tautology coverage.

## Changes

- Added a real RRD semantic decoder to `crates/roz-cli/tests/mcap_to_rrd.rs` using `DecoderApp::decode_lazy` and `ChunkBatch::try_from`.
- Added `semantic_rrd_contains_entities_timelines_and_components`, which synthesizes an MCAP with `/roz/log`, `/tf`, `/roz/telemetry/pose`, and `/roz/camera/front`, runs the real `roz mcap to-rrd` subprocess, and asserts expected Rerun entities, `publish_time`/`log_time` timelines, and text/transform/video components.
- Extended the existing bulk-mode test so successful `.rrd` outputs must decode to `/session/log` semantics, not just `RRF2` bytes.
- Added `crates/roz-worker/tests/camera_mcap_relay_vertical.rs`, an ignored container-backed vertical that routes `TestPatternSource` -> `SwEncoder` -> `StreamHub` -> `spawn_mcap_relay` -> NATS -> `spawn_edge_ingestors` -> server `WriterActor` -> MCAP, then verifies `/roz/camera/wrist_cam` as `foxglove.CompressedVideo`.
- Added pinned Rerun read-side dev dependencies for `roz-cli` tests: `re_log_encoding`, `re_log_types`, and `re_sorbet` at `=0.31.3`.

## Verification

- `rustfmt --edition 2024 crates/roz-cli/tests/mcap_to_rrd.rs crates/roz-worker/tests/camera_mcap_relay_vertical.rs`
- `cargo test -p roz-cli --features export-rrd --test mcap_to_rrd`
  - Passed: 2 passed, 1 ignored.
- `cargo test -p roz-worker --features test-fixtures --test camera_mcap_relay_vertical --no-run`
  - Passed compile.
- `cargo test -p roz-worker --features test-fixtures --test camera_mcap_relay_vertical -- --ignored --test-threads=1 --nocapture`
  - Failed before assertions because local Docker/testcontainers timed out creating the Postgres container: `Client(CreateContainer(RequestTimeoutError))`.

## Notes

- The camera vertical avoids direct camera writer injection; it does not call `ChannelKey::Camera` or send a camera `WriteCommand::Event` from the test.
- The ignored test's runtime failure is infrastructure-local. The test target compiles and remains wired for CI environments where Postgres/NATS testcontainers can start reliably.
