---
phase: "26.11"
plan: "04"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/roz-worker/tests/camera_mcap_relay_vertical.rs
  - crates/roz-cli/Cargo.toml
  - crates/roz-cli/tests/mcap_to_rrd.rs
autonomous: true
requirements: [TR-04]
tags: [camera, mcap, rrd, rerun, semantic-validation, worker-relay]

must_haves:
  truths:
    - "Camera archival acceptance uses worker `spawn_mcap_relay` -> NATS camera subject -> server `spawn_edge_ingestors`/camera ingest -> `WriterActor` -> MCAP."
    - "Camera vertical uses structurally valid H.264 Annex-B bytes from `TestPatternSource` + `SwEncoder`, not arbitrary hand-rolled bytes."
    - "Camera acceptance asserts decoded MCAP `/roz/camera/{name}` messages, not only file existence."
    - "RRD tests decode the produced `.rrd` and assert entity paths, timelines, and component columns beyond `RRF2` magic."
    - "Rerun semantic inspection uses exact pinned 0.31.3 dev-dependencies."
  artifacts:
    - path: "crates/roz-worker/tests/camera_mcap_relay_vertical.rs"
      provides: "worker relay to MCAP camera vertical"
      contains: "camera_relay_writes_decodable_h264_to_mcap"
    - path: "crates/roz-cli/Cargo.toml"
      provides: "exact pinned Rerun read-side dev deps"
      contains: "re_log_encoding"
    - path: "crates/roz-cli/tests/mcap_to_rrd.rs"
      provides: "semantic RRD assertions"
      contains: "rrd_entities_timelines_components"
  key_links:
    - from: "crates/roz-worker/tests/camera_mcap_relay_vertical.rs"
      to: "crates/roz-worker/src/camera/mcap_relay.rs"
      via: "spawn_mcap_relay"
      pattern: "spawn_mcap_relay"
    - from: "crates/roz-worker/tests/camera_mcap_relay_vertical.rs"
      to: "crates/roz-server/src/observability/ingest_edge.rs"
      via: "spawn_edge_ingestors"
      pattern: "spawn_edge_ingestors"
    - from: "crates/roz-cli/tests/mcap_to_rrd.rs"
      to: "re_log_encoding::DecoderApp"
      via: "direct RRD semantic decode"
      pattern: "DecoderApp::decode_lazy"
---

<objective>
Close the camera and RRD anti-tautology gaps: prove camera frames traverse the worker relay path into MCAP, and prove `.rrd` output contains expected semantic entities/timelines/components.

Purpose: Current camera tests directly inject writer commands and current RRD tests mostly check `RRF2` magic. This plan replaces those acceptance gaps with real production-path and semantic evidence.
Output: One camera vertical test and upgraded RRD semantic tests using exact Rerun 0.31.3 ecosystem dev-dependencies.
</objective>

<execution_context>
@/Users/krnzt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/krnzt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
@.planning/phases/26.9-rrd-format-export-static-rerun-recording-files-for-substrate/26.9-VERIFICATION.md
@crates/roz-worker/src/camera/mcap_relay.rs
@crates/roz-server/src/observability/ingest_edge.rs
@crates/roz-worker/tests/camera_integration.rs
@crates/roz-server/tests/mcap_camera_roundtrip.rs
@crates/roz-cli/tests/mcap_to_rrd.rs
@crates/roz-cli/src/commands/mcap/export.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add worker relay -> NATS -> server ingest -> MCAP camera vertical</name>
  <files>crates/roz-worker/tests/camera_mcap_relay_vertical.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - crates/roz-worker/src/camera/mcap_relay.rs
    - crates/roz-server/src/observability/ingest_edge.rs
    - crates/roz-server/src/observability/mcap_archive.rs
    - crates/roz-worker/tests/camera_integration.rs
    - crates/roz-server/tests/mcap_camera_roundtrip.rs
    - crates/roz-worker/Cargo.toml
  </read_first>
  <behavior>
    - Test `camera_relay_writes_decodable_h264_to_mcap`: generate one TestPattern frame, encode with `SwEncoder`, publish through `StreamHub`, run `spawn_mcap_relay`, ingest via server edge ingestors, finalize MCAP, and decode `/roz/camera/wrist_cam`.
  </behavior>
  <action>
    Create `crates/roz-worker/tests/camera_mcap_relay_vertical.rs`.

    Mark the test explicitly ignored because it uses NATS/Postgres/MCAP relay containers and Plan 01 routes this binary through ignored-only nightly nextest:

    ```rust
    #[tokio::test]
    #[ignore = "requires NATS/Postgres camera relay containers; routed by ci-integration nextest"]
    async fn camera_relay_writes_decodable_h264_to_mcap() {
        // ...
    }
    ```

    Test setup must use existing components:
    - NATS testcontainer via `roz_test::nats_container()`.
    - Postgres testcontainer + migrations for `WriterActor` archive rows.
    - `TestPatternSource` and `SwEncoder` from `crates/roz-worker/tests/camera_integration.rs` pattern.
    - `StreamHub::register_camera` + `StreamHub::publish`.
    - `spawn_mcap_relay` from `crates/roz-worker/src/camera/mcap_relay.rs`.
    - `spawn_edge_ingestors` from `crates/roz-server/src/observability/ingest_edge.rs`.
    - `spawn_writer` and `WriteCommand::Finalize` from `mcap_archive`.

    Use camera id `wrist_cam`, worker id `worker-camera-vertical`, and a UUID session id string. The test must not call `WriteCommand::Event { channel: ChannelKey::Camera(..) }` directly. The only path into the writer for camera messages must be `spawn_edge_ingestors`.

    Assertions:
    - MCAP contains a channel topic exactly `/roz/camera/wrist_cam`.
    - Channel schema name is exactly `foxglove.CompressedVideo`.
    - At least one camera message decodes as `foxglove.CompressedVideo`.
    - Decoded `format == "h264"`.
    - Decoded `frame_id == "wrist_cam"`.
    - Decoded `data` begins with Annex-B start code `00 00 00 01` or `00 00 01`.
    - Archive DB row for the session is finalized and has non-null digest and size greater than 0.

    Use bounded timeouts for relay publication, ingest visibility, and archive finalization. Do not use sleeps as the sole synchronization.
  </action>
  <verify>
    <automated>cargo test -p roz-worker --test camera_mcap_relay_vertical -- --ignored --test-threads=1 --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "spawn_mcap_relay" crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns at least one line.
    - `grep -F "spawn_edge_ingestors" crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns at least one line.
    - `grep -F "SwEncoder" crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns at least one line.
    - `grep -F "/roz/camera/wrist_cam" crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns at least one line.
    - `grep -F "foxglove.CompressedVideo" crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns at least one line.
    - `grep -F '#[ignore = "requires NATS/Postgres camera relay containers; routed by ci-integration nextest"]' crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns one line.
    - `grep -E "ChannelKey::Camera|WriteCommand::Event.*Camera" crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns no lines.
    - `grep -F "tokio::time::timeout" crates/roz-worker/tests/camera_mcap_relay_vertical.rs` returns at least one line.
    - `cargo test -p roz-worker --test camera_mcap_relay_vertical -- --ignored --test-threads=1 --nocapture` exits 0.
  </acceptance_criteria>
  <done>Camera archival is proven through worker relay and server ingest, not direct writer injection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade RRD tests from magic-header smoke to semantic decode</name>
  <files>crates/roz-cli/Cargo.toml, crates/roz-cli/tests/mcap_to_rrd.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
    - .planning/phases/26.9-rrd-format-export-static-rerun-recording-files-for-substrate/26.9-VERIFICATION.md
    - crates/roz-cli/Cargo.toml
    - crates/roz-cli/tests/mcap_to_rrd.rs
    - crates/roz-cli/src/commands/mcap/export.rs
    - crates/roz-cli/src/commands/mcap/recording.rs
    - crates/roz-cli/src/commands/mcap/text_logs.rs
    - crates/roz-cli/src/commands/mcap/transforms.rs
    - crates/roz-cli/src/commands/mcap/camera.rs
  </read_first>
  <behavior>
    - Test `semantic_rrd_contains_entities_timelines_and_components`: convert a synthetic MCAP with `/roz/log`, `/tf`, `/roz/telemetry/pose`, and `/roz/camera/front`; decode the `.rrd`; assert expected entity paths, timelines, and component names exist.
    - Existing `bulk_mode_continue_on_error` still asserts `RRF2` magic, but now also uses semantic decode for successful outputs.
  </behavior>
  <action>
    Add exact dev-dependencies to `crates/roz-cli/Cargo.toml`:

    ```toml
    re_log_encoding = "=0.31.3"
    re_log_types = "=0.31.3"
    re_sorbet = "=0.31.3"
    ```

    Keep them in `[dev-dependencies]`. Do not make them runtime deps.

    Extend `crates/roz-cli/tests/mcap_to_rrd.rs` with the research helper:

    ```rust
    fn rrd_entities_timelines_components(path: &Path) -> anyhow::Result<(BTreeSet<String>, BTreeSet<String>, BTreeSet<String>)>
    ```

    Use `re_log_encoding::DecoderApp::decode_lazy`, match `re_log_types::LogMsg::ArrowMsg`, convert to `re_sorbet::ChunkBatch`, and collect entity path, timeline names, and component paths.

    Add `semantic_rrd_contains_entities_timelines_and_components` that:
    - writes a synthetic MCAP with at least `/roz/log`, `/tf`, `/roz/telemetry/pose`, and `/roz/camera/front`;
    - invokes the actual `roz mcap to-rrd` subprocess with `--features export-rrd` test binary path just like existing tests;
    - decodes the output `.rrd`;
    - asserts entities contain `/session/log`, `/world/robot/pose`, and `/world/cameras/front`;
    - asserts timelines contain both `publish_time` and `log_time`;
    - asserts component paths include at least one text-log component and one transform/video-related component from the decoded chunks.

    Leave `smoke_to_rrd_single_file` ignored unless the fixture exists; do not make a human-only viewer check mandatory.
  </action>
  <verify>
    <automated>cargo test -p roz-cli --features export-rrd --test mcap_to_rrd semantic_rrd_contains_entities_timelines_and_components -- --nocapture</automated>
    <automated>cargo test -p roz-cli --features export-rrd --test mcap_to_rrd bulk_mode_continue_on_error -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 're_log_encoding = "=0.31.3"' crates/roz-cli/Cargo.toml` returns one line.
    - `grep -F 're_log_types = "=0.31.3"' crates/roz-cli/Cargo.toml` returns one line.
    - `grep -F 're_sorbet = "=0.31.3"' crates/roz-cli/Cargo.toml` returns one line.
    - `grep -F "DecoderApp::decode_lazy" crates/roz-cli/tests/mcap_to_rrd.rs` returns at least one line.
    - `grep -F "ChunkBatch::try_from" crates/roz-cli/tests/mcap_to_rrd.rs` returns at least one line.
    - `grep -F "semantic_rrd_contains_entities_timelines_and_components" crates/roz-cli/tests/mcap_to_rrd.rs` returns one line.
    - `grep -F "/session/log" crates/roz-cli/tests/mcap_to_rrd.rs` returns at least one line.
    - `grep -F "publish_time" crates/roz-cli/tests/mcap_to_rrd.rs` and `grep -F "log_time" crates/roz-cli/tests/mcap_to_rrd.rs` both return lines.
    - `cargo test -p roz-cli --features export-rrd --test mcap_to_rrd semantic_rrd_contains_entities_timelines_and_components -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>RRD automated evidence checks semantic contents, not just the file header.</done>
</task>

</tasks>

<verification>
- `cargo fmt --check`
- `cargo test -p roz-worker --test camera_mcap_relay_vertical -- --ignored --test-threads=1 --nocapture`
- `cargo test -p roz-cli --features export-rrd --test mcap_to_rrd -- --nocapture`
</verification>

<success_criteria>
- Camera archive path is vertical through worker relay/NATS/server ingest/MCAP.
- RRD validation decodes entity paths, timelines, and component columns under exact Rerun 0.31.3 packages.
</success_criteria>

<output>
After completion, create `.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-04-SUMMARY.md`.
</output>
