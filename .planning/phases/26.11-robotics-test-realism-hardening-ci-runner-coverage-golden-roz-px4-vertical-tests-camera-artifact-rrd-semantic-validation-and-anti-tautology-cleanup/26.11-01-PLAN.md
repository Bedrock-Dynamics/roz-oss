---
phase: "26.11"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/nightly.yml
  - .config/nextest.toml
  - crates/roz-mavlink/src/backend.rs
  - crates/roz-mavlink/src/transport/mod.rs
  - crates/roz-mavlink/tests/log_fanout.rs
  - crates/roz-mavlink/tests/send_log_erase.rs
  - crates/roz-mavlink/tests/autopilot_kind.rs
  - crates/roz-mavlink/tests/qgc_coexistence.rs
  - crates/roz-worker/tests/mavlink_backend_null_key.rs
autonomous: true
requirements: [TR-01]
tags: [test-runner, ci, nightly, mavlink, process-exit, anti-tautology]

must_haves:
  truths:
    - "PR CI has explicit bounded jobs for `roz-server --features test-helpers`, `roz-cli --features export-rrd`, `roz-worker --features test-fixtures`, and `roz-mavlink --features test-helpers` per 26.11-CONTEXT Test Runner + CI Scope."
    - "Nightly nextest routes ignored/container robotics tests by named binary allowlists; newly ignored tests from 26.11 are not orphaned."
    - "Normal MAVLink and worker test binaries no longer call `std::process::exit(0)`; one test cannot terminate the rest of a binary."
    - "Expensive PX4/SITL/hardware bench paths remain nightly or env-gated; no real hardware is added to default PR `cargo test --workspace`."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "bounded feature-specific PR jobs"
      contains: "roz-cli --features export-rrd"
    - path: ".github/workflows/nightly.yml"
      provides: "nightly feature/ignored routing"
      contains: "robotics-test-realism"
    - path: ".config/nextest.toml"
      provides: "allowlist for ignored robotics acceptance binaries"
      contains: "manipulator_harness_vertical"
    - path: "crates/roz-mavlink/src/transport/mod.rs"
      provides: "cancelable/drop-clean MAVLink transport test teardown"
      contains: "TransportHandle"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "feature-gated test crates"
      via: "cargo test commands with required features"
      pattern: "cargo test -p roz-server --features test-helpers"
    - from: ".config/nextest.toml"
      to: "26.11 ignored tests"
      via: "binary(...) allowlist"
      pattern: "binary\\(camera_mcap_relay_vertical\\)"
    - from: "crates/roz-mavlink/tests/*"
      to: "crates/roz-mavlink/src/transport/mod.rs"
      via: "drop/cancel cleanup instead of process exit"
      pattern: "std::process::exit\\(0\\)"
---

<objective>
Harden the runner before adding new robotics acceptance tests: make feature-gated suites visible to PR/nightly CI and remove process-wide exits from MAVLink tests.

Purpose: The audit found high-value tests skipped by default and MAVLink tests that can hide failures by terminating the whole test binary. This plan makes later 26.11 tests observable without adding PX4/SITL/hardware bench to PR default.
Output: CI/nightly routing plus MAVLink test teardown that is reliable enough to count as evidence.
</objective>

<execution_context>
@/Users/krnzt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/krnzt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
@.github/workflows/ci.yml
@.github/workflows/nightly.yml
@.config/nextest.toml
@crates/roz-mavlink/src/backend.rs
@crates/roz-mavlink/src/transport/mod.rs
@crates/roz-mavlink/tests/qgc_coexistence.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route feature-gated robotics tests in PR and nightly CI</name>
  <files>.github/workflows/ci.yml, .github/workflows/nightly.yml, .config/nextest.toml</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
    - .planning/codebase/TESTING.md
    - .github/workflows/ci.yml
    - .github/workflows/nightly.yml
    - .config/nextest.toml
    - crates/roz-server/Cargo.toml
    - crates/roz-cli/Cargo.toml
    - crates/roz-worker/Cargo.toml
    - crates/roz-mavlink/Cargo.toml
  </read_first>
  <action>
    Add a bounded PR job named `robotics-feature-tests` in `.github/workflows/ci.yml` after the existing feature jobs. Reuse the pinned Rust/protoc/cache pattern already in the file. The job must run exactly these commands:

    - `cargo test -p roz-server --features test-helpers`
    - `cargo test -p roz-cli --features export-rrd --test mcap_to_rrd`
    - `cargo test -p roz-worker --features test-fixtures`
    - `cargo test -p roz-mavlink --features test-helpers --tests`

    Keep this PR job free of `--ignored`, PX4 SITL, and hardware bench env vars.

    Extend `.config/nextest.toml` `profile.ci-integration.default-filter` with binary allowlist entries for the ignored/container tests this phase owns: `manipulator_harness_vertical`, `safety_policy_physical_vertical`, `camera_mcap_relay_vertical`, `session_bundle_export_live`, `copper_artifact_finalize`, `artifact_copper_roundtrip`, `export_roundtrip`, `mcap_camera_roundtrip`, `mcap_agent_session_live`, `session_metadata_fleet_query`, `px4_readiness_replay`, and `px4_fixture_record_verify`. Existing binaries must remain listed.

    Add a nightly job or steps in `.github/workflows/nightly.yml` labeled `robotics-test-realism` that run ignored-only nextest for the feature-gated crates without real hardware:

    - `cargo nextest run --profile ci-integration --run-ignored ignored-only -p roz-server --features test-helpers`
    - `cargo nextest run --profile ci-integration --run-ignored ignored-only -p roz-cli --features export-rrd`
    - `cargo nextest run --profile ci-integration --run-ignored ignored-only -p roz-worker --features test-fixtures`
    - `cargo nextest run --profile ci-integration --run-ignored ignored-only -p roz-mavlink --features test-helpers`

    Do not set `ROZ_MANIPULATOR_HARDWARE=1`, `ROZ_PX4_SITL=1`, or any real-hardware env var in PR jobs. If a new ignored test is introduced later in this phase, it must match one of the binary names above or this task must be extended before the test is added.
  </action>
  <verify>
    <automated>cargo test -p roz-cli --features export-rrd --test mcap_to_rrd</automated>
    <automated>cargo test -p roz-worker --features test-fixtures --no-run</automated>
    <automated>cargo test -p roz-server --features test-helpers --no-run</automated>
    <automated>cargo test -p roz-mavlink --features test-helpers --tests --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "cargo test -p roz-server --features test-helpers" .github/workflows/ci.yml` returns at least one line.
    - `grep -F "cargo test -p roz-cli --features export-rrd --test mcap_to_rrd" .github/workflows/ci.yml` returns at least one line, and the matched command must not append a single test-name filter.
    - `grep -F "cargo test -p roz-worker --features test-fixtures" .github/workflows/ci.yml` returns at least one line.
    - `grep -F "cargo test -p roz-mavlink --features test-helpers --tests" .github/workflows/ci.yml` returns at least one line.
    - `grep -F "binary(manipulator_harness_vertical)" .config/nextest.toml` returns one line.
    - `grep -F "binary(camera_mcap_relay_vertical)" .config/nextest.toml` returns one line.
    - `grep -F "binary(copper_artifact_finalize)" .config/nextest.toml` returns one line.
    - `grep -F "robotics-test-realism" .github/workflows/nightly.yml` returns at least one line.
    - `grep -E "ROZ_MANIPULATOR_HARDWARE=1|ROZ_PX4_SITL=1" .github/workflows/ci.yml` returns no lines.
  </acceptance_criteria>
  <done>Feature-gated 26.x robotics tests are routed in PR or nightly, and ignored tests have named nextest routes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove process-wide exit from normal MAVLink test binaries</name>
  <files>crates/roz-mavlink/src/backend.rs, crates/roz-mavlink/src/transport/mod.rs, crates/roz-mavlink/tests/log_fanout.rs, crates/roz-mavlink/tests/send_log_erase.rs, crates/roz-mavlink/tests/autopilot_kind.rs, crates/roz-mavlink/tests/qgc_coexistence.rs, crates/roz-worker/tests/mavlink_backend_null_key.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
    - crates/roz-mavlink/src/backend.rs
    - crates/roz-mavlink/src/transport/mod.rs
    - crates/roz-mavlink/tests/log_fanout.rs
    - crates/roz-mavlink/tests/send_log_erase.rs
    - crates/roz-mavlink/tests/autopilot_kind.rs
    - crates/roz-mavlink/tests/qgc_coexistence.rs
    - crates/roz-worker/tests/mavlink_backend_null_key.rs
  </read_first>
  <action>
    Replace the current `std::process::exit(0)` teardown pattern in MAVLink tests with bounded cleanup.

    Implement a narrow teardown seam in `crates/roz-mavlink/src/transport/mod.rs` and/or `crates/roz-mavlink/src/backend.rs` so tests can terminate the UDP reader/writer/router tasks without killing the process. Acceptable implementation shapes:

    - Add `TransportHandle::shutdown(self)` that aborts the writer task, drops the outbound channel, and unblocks the reader by dropping/closing the connection state it owns; or
    - Add a `CancellationToken` or atomic stop flag checked around the blocking `conn.recv()` loop plus a UDP self-nudge on shutdown.

    Whichever shape is chosen, add `MavlinkBackend::shutdown_for_tests(self) -> impl Future<Output = ()>` or a synchronous equivalent gated with `#[doc(hidden)]` if it is public. Tests must call it instead of `std::process::exit(0)`.

    Update these test files so every test can run to completion inside its normal binary:

    - `crates/roz-mavlink/tests/log_fanout.rs`
    - `crates/roz-mavlink/tests/send_log_erase.rs`
    - `crates/roz-mavlink/tests/autopilot_kind.rs`
    - `crates/roz-mavlink/tests/qgc_coexistence.rs`
    - `crates/roz-worker/tests/mavlink_backend_null_key.rs`

    Preserve their existing assertions. For `qgc_coexistence.rs`, both `copper_and_qgc_shim_coexist_unsigned` and `copper_and_qgc_shim_coexist_signed` must run in one `cargo test -p roz-mavlink --test qgc_coexistence` invocation. Do not hide either variant behind `#[ignore]`.
  </action>
  <verify>
    <automated>cargo test -p roz-mavlink --test autopilot_kind --features test-helpers</automated>
    <automated>cargo test -p roz-mavlink --test log_fanout --features test-helpers</automated>
    <automated>cargo test -p roz-mavlink --test qgc_coexistence --features test-helpers</automated>
    <automated>cargo test -p roz-worker --test mavlink_backend_null_key</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "std::process::exit(0)" crates/roz-mavlink/tests crates/roz-worker/tests/mavlink_backend_null_key.rs` returns no lines.
    - `cargo test -p roz-mavlink --test qgc_coexistence --features test-helpers` exits 0 and output includes both `copper_and_qgc_shim_coexist_unsigned` and `copper_and_qgc_shim_coexist_signed`.
    - `cargo test -p roz-mavlink --test log_fanout --features test-helpers` exits 0 without hanging after assertions.
    - `cargo test -p roz-worker --test mavlink_backend_null_key` exits 0 without hanging after assertions.
  </acceptance_criteria>
  <done>MAVLink tests are reliable CI evidence because no normal test binary can terminate the process before later tests run.</done>
</task>

</tasks>

<verification>
- `cargo fmt --check`
- `cargo test -p roz-cli --features export-rrd --test mcap_to_rrd`
- `cargo test -p roz-mavlink --features test-helpers --tests`
- `grep -R "std::process::exit(0)" crates/roz-mavlink/tests crates/roz-worker/tests/mavlink_backend_null_key.rs` returns no lines
</verification>

<success_criteria>
- Feature-gated test suites are named in CI/nightly.
- No new expensive PX4/SITL/hardware bench work runs in PR default.
- MAVLink process-exit anti-pattern is removed from normal test binaries.
</success_criteria>

<output>
After completion, create `.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-01-SUMMARY.md`.
</output>
