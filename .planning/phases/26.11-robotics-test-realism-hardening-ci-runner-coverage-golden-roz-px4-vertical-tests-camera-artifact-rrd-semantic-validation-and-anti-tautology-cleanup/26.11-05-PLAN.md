---
phase: "26.11"
plan: "05"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/roz-worker/tests/copper_artifact_finalize.rs
  - crates/roz-cli/Cargo.toml
  - crates/roz-cli/src/commands/session.rs
  - crates/roz-cli/tests/session_bundle_export_live.rs
autonomous: true
requirements: [TR-05]
tags: [artifacts, bundle, cli, vertical-test, copper-segments]

must_haves:
  truths:
    - "Copper archival uploads one artifact row per `.copper` segment."
    - "Successful copper archival removes local session dir when `keep_local_after_upload=false`."
    - "Soft-failed copper archival retains local session dir for retry."
    - "`roz session export --bundle` is tested by launching the actual `roz` CLI binary from a `roz-cli` integration test, with loopback ObservabilityService and ArtifactService responses."
    - "Bundle tar has `manifest.json` first, includes MCAP rows and artifact rows, and every digest verifies."
  artifacts:
    - path: "crates/roz-worker/tests/copper_artifact_finalize.rs"
      provides: "worker finalizer segment/copy/retention vertical"
      contains: "uploads_one_artifact_row_per_copper_segment"
    - path: "crates/roz-cli/tests/session_bundle_export_live.rs"
      provides: "live CLI bundle export test using same-package binary path"
      contains: "cli_bundle_export_includes_mcap_and_artifacts_with_verified_digests"
    - path: "crates/roz-cli/Cargo.toml"
      provides: "test-only access to roz-server generated gRPC server traits"
      contains: 'roz-server = { path = "../roz-server" }'
    - path: "crates/roz-cli/src/commands/session.rs"
      provides: "localhost/plaintext testable session channel if needed"
      contains: "export_bundle"
  key_links:
    - from: "crates/roz-worker/tests/copper_artifact_finalize.rs"
      to: "crates/roz-worker/src/copper_archive.rs"
      via: "finalize_copper_archive"
      pattern: "finalize_copper_archive"
    - from: "crates/roz-cli/tests/session_bundle_export_live.rs"
      to: "crates/roz-cli/src/commands/session.rs"
      via: "actual same-package `roz session export --bundle` subprocess"
      pattern: "--bundle"
    - from: "crates/roz-cli/src/commands/session.rs"
      to: "ArtifactService.ListSessionArtifacts + DownloadArtifact"
      via: "fetch_artifact_entries"
      pattern: "ListSessionArtifactsRequest"
---

<objective>
Add artifact and bundle vertical evidence: copper segment finalization uploads exactly one row per segment, and actual CLI bundle export packages MCAP plus artifacts with verified digests.

Purpose: Existing tests prove ArtifactService and tar helpers separately. This plan proves worker finalizer behavior and the CLI bundle boundary operators use, with the CLI test located in the `roz-cli` crate so `CARGO_BIN_EXE_roz` resolves to the real binary.
Output: Worker finalizer integration tests and a live CLI bundle test.
</objective>

<execution_context>
@/Users/krnzt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/krnzt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
@crates/roz-worker/src/copper_archive.rs
@crates/roz-server/tests/artifact_copper_roundtrip.rs
@crates/roz-cli/src/commands/session.rs
@crates/roz-cli/src/config.rs
@crates/roz-server/tests/export_roundtrip.rs
@crates/roz-worker/tests/ulog_session_finalize_integration.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add copper segment finalizer vertical tests</name>
  <files>crates/roz-worker/tests/copper_artifact_finalize.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - crates/roz-worker/src/copper_archive.rs
    - crates/roz-server/tests/artifact_copper_roundtrip.rs
    - crates/roz-worker/tests/ulog_session_finalize_integration.rs
    - crates/roz-worker/src/observability_config.rs
    - crates/roz-db/src/session_artifacts.rs
  </read_first>
  <behavior>
    - Test `uploads_one_artifact_row_per_copper_segment`: create `session_0.copper` and `session_1.copper`, call `finalize_copper_archive`, assert exactly two DB artifact rows with type `copper`, paths matching both segment names, and digests matching file bytes.
    - Test `successful_copper_archive_cleanup_removes_session_dir`: with `keep_local_after_upload=false`, successful upload removes `{data_dir}/sessions/{session_id}`.
    - Test `soft_failed_copper_archive_retains_session_dir`: a mock ArtifactService failure for one segment returns `Ok(())` from finalizer and leaves the local session dir present.
  </behavior>
  <action>
    Create `crates/roz-worker/tests/copper_artifact_finalize.rs`.

    Mark each container-backed test explicitly ignored because Plan 01 routes this binary through ignored-only nightly nextest:

    ```rust
    #[tokio::test]
    #[ignore = "requires ArtifactService/Postgres loopback; routed by ci-integration nextest"]
    async fn uploads_one_artifact_row_per_copper_segment() {
        // ...
    }
    ```

    Reuse the tonic loopback ArtifactService harness shape from `artifact_copper_roundtrip.rs` or the mock service shape from `ulog_session_finalize_integration.rs`. Prefer real `ArtifactServiceImpl` + Postgres for the success tests so DB rows are asserted through `roz_db::session_artifacts`.

    Build fixture files under `{tmp}/sessions/{session_id}/`:
    - `session_0.copper` bytes: `b"copper-segment-zero"`
    - `session_1.copper` bytes: `b"copper-segment-one"`

    Call `finalize_copper_archive(&data_dir, &session_id, &ObservabilityCopperConfig { enabled: true, keep_local_after_upload: false, ..default }, client)`.

    Assertions:
    - exactly two rows from `roz_db::session_artifacts::list_by_session`;
    - both rows have `artifact_type == "copper"`;
    - row paths are `session_0.copper` and `session_1.copper`;
    - each row digest equals SHA-256 of its file bytes;
    - success cleanup removes the session dir;
    - soft-failure branch retains the session dir and `finalize_copper_archive` still returns `Ok(())`.
  </action>
  <verify>
    <automated>cargo test -p roz-worker --test copper_artifact_finalize -- --ignored --test-threads=1 --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "uploads_one_artifact_row_per_copper_segment" crates/roz-worker/tests/copper_artifact_finalize.rs` returns one line.
    - `grep -F "finalize_copper_archive" crates/roz-worker/tests/copper_artifact_finalize.rs` returns at least one line.
    - `grep -F "session_0.copper" crates/roz-worker/tests/copper_artifact_finalize.rs` and `grep -F "session_1.copper" crates/roz-worker/tests/copper_artifact_finalize.rs` both return lines.
    - `grep -F '#[ignore = "requires ArtifactService/Postgres loopback; routed by ci-integration nextest"]' crates/roz-worker/tests/copper_artifact_finalize.rs` returns at least one line.
    - `grep -F "keep_local_after_upload" crates/roz-worker/tests/copper_artifact_finalize.rs` returns at least one line.
    - `grep -F "list_by_session" crates/roz-worker/tests/copper_artifact_finalize.rs` returns at least one line.
    - `cargo test -p roz-worker --test copper_artifact_finalize -- --ignored --test-threads=1 --nocapture` exits 0.
  </acceptance_criteria>
  <done>Copper finalizer tests prove segment count, digests, cleanup, and soft-fail retention through the upload path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add live CLI bundle export test in the CLI crate</name>
  <files>crates/roz-cli/Cargo.toml, crates/roz-cli/src/commands/session.rs, crates/roz-cli/tests/session_bundle_export_live.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - crates/roz-cli/Cargo.toml
    - crates/roz-cli/build.rs
    - crates/roz-cli/src/commands/session.rs
    - crates/roz-cli/src/config.rs
    - crates/roz-server/tests/artifact_copper_roundtrip.rs
    - crates/roz-server/tests/observability_export_grpc.rs
    - crates/roz-server/tests/export_roundtrip.rs
    - crates/roz-cli/tests/mcap_to_rrd.rs
  </read_first>
  <behavior>
    - Test `cli_bundle_export_includes_mcap_and_artifacts_with_verified_digests`: boot loopback tonic services implementing ObservabilityService.ExportSession plus ArtifactService.ListSessionArtifacts/DownloadArtifact, run the actual same-package `roz` binary with `session export <session_id> --format mcap --bundle --output <tar>`, and verify tar manifest/digests.
  </behavior>
  <action>
    Create `crates/roz-cli/tests/session_bundle_export_live.rs`. This test belongs in `roz-cli` specifically so `env!("CARGO_BIN_EXE_roz")` points at the same-package CLI binary; do not place this test in `crates/roz-server/tests`.

    Add a test-only dev-dependency to `crates/roz-cli/Cargo.toml` so the test can use the server-side tonic traits that `roz-cli` intentionally does not generate:

    ```toml
    [dev-dependencies]
    roz-server = { path = "../roz-server" }
    ```

    Do not change `crates/roz-cli/build.rs` from `build_server(false)`; CLI production code should remain client-only.

    Build a loopback tonic server inside the test using `roz-server`'s generated server traits:
    - `roz_server::grpc::roz_v1::observability_service_server::ObservabilityService`
    - `roz_server::grpc::roz_v1::observability_service_server::ObservabilityServiceServer`
    - `roz_server::grpc::roz_v1::artifact_service_server::ArtifactService`
    - `roz_server::grpc::roz_v1::artifact_service_server::ArtifactServiceServer`

    The subprocess CLI still uses `roz-cli`'s generated client types over the wire; the test server may use `roz-server` generated types because the protobuf wire schema is identical.

    Mark the CLI subprocess loopback test explicitly ignored because it boots a tonic server and Plan 01 routes this binary through ignored-only nightly nextest:

    ```rust
    #[tokio::test]
    #[ignore = "requires loopback gRPC services and CLI subprocess; routed by ci-integration nextest"]
    async fn cli_bundle_export_includes_mcap_and_artifacts_with_verified_digests() {
        // ...
    }
    ```

    The loopback service must:
    - return a real non-empty MCAP byte stream from `ExportSession`; bytes must be readable by `mcap::MessageStream`;
    - return two artifact summaries from `ListSessionArtifacts` with type `copper` and paths `session_0.copper`, `session_1.copper`;
    - return matching bytes and final-frame `digest_sha256` values from `DownloadArtifact` for both artifacts;
    - assert incoming requests carry the `authorization` metadata populated from the CLI config.

    Launch the actual CLI binary with:

    ```rust
    let mut cmd = std::process::Command::new(env!("CARGO_BIN_EXE_roz"));
    cmd.env("ROZ_API_URL", loopback_url);
    cmd.env("ROZ_API_KEY", "test-token");
    cmd.args([
        "session", "export", session_id.as_str(),
        "--format", "mcap",
        "--bundle",
        "--output", bundle_path.to_str().unwrap(),
    ]);
    ```

    If `crates/roz-cli/src/commands/session.rs::session_channel` currently forces TLS for `http://127.0.0.1`, update it narrowly:
    - apply `ClientTlsConfig` only when `config.api_url.starts_with("https://")`;
    - leave HTTPS behavior unchanged;
    - add/adjust a unit test if one exists for URL scheme handling.

    Bundle assertions:
    - first tar entry path is exactly `manifest.json`;
    - manifest JSON `files` includes exactly one path beginning `mcap/` and two copper paths `copper/session_0.copper`, `copper/session_1.copper`;
    - every listed file exists in the tar;
    - SHA-256 of each tar entry equals manifest `digest_sha256`;
    - `manifest.json` is not duplicated later in the archive.
  </action>
  <verify>
    <automated>cargo test -p roz-cli --test session_bundle_export_live -- --ignored --test-threads=1 --nocapture</automated>
    <automated>cargo test -p roz-cli --lib commands::session</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "cli_bundle_export_includes_mcap_and_artifacts_with_verified_digests" crates/roz-cli/tests/session_bundle_export_live.rs` returns one line.
    - `grep -F 'roz-server = { path = "../roz-server" }' crates/roz-cli/Cargo.toml` returns one line under `[dev-dependencies]`.
    - `grep -F "roz_server::grpc::roz_v1::observability_service_server::ObservabilityServiceServer" crates/roz-cli/tests/session_bundle_export_live.rs` returns at least one line.
    - `grep -F "roz_server::grpc::roz_v1::artifact_service_server::ArtifactServiceServer" crates/roz-cli/tests/session_bundle_export_live.rs` returns at least one line.
    - `grep -F "build_server(false)" crates/roz-cli/build.rs` returns one line.
    - `grep -F 'env!("CARGO_BIN_EXE_roz")' crates/roz-cli/tests/session_bundle_export_live.rs` returns one line.
    - `grep -F '#[ignore = "requires loopback gRPC services and CLI subprocess; routed by ci-integration nextest"]' crates/roz-cli/tests/session_bundle_export_live.rs` returns one line.
    - `grep -F "ROZ_API_URL" crates/roz-cli/tests/session_bundle_export_live.rs` and `grep -F "ROZ_API_KEY" crates/roz-cli/tests/session_bundle_export_live.rs` both return lines.
    - `grep -F "--bundle" crates/roz-cli/tests/session_bundle_export_live.rs` returns at least one line.
    - `grep -F "manifest.json" crates/roz-cli/tests/session_bundle_export_live.rs` returns at least one line.
    - `grep -F "copper/session_0.copper" crates/roz-cli/tests/session_bundle_export_live.rs` and `grep -F "copper/session_1.copper" crates/roz-cli/tests/session_bundle_export_live.rs` both return lines.
    - `grep -F "digest_sha256" crates/roz-cli/tests/session_bundle_export_live.rs` returns at least one line.
    - `grep -R "session_bundle_export_live" crates/roz-server/tests crates/roz-server/Cargo.toml` returns no lines.
    - `cargo test -p roz-cli --test session_bundle_export_live -- --ignored --test-threads=1 --nocapture` exits 0.
  </acceptance_criteria>
  <done>Bundle export is proven through the actual same-package CLI binary path with manifest-first and digest checks.</done>
</task>

</tasks>

<verification>
- `cargo fmt --check`
- `cargo test -p roz-worker --test copper_artifact_finalize -- --ignored --test-threads=1 --nocapture`
- `cargo test -p roz-cli --test session_bundle_export_live -- --ignored --test-threads=1 --nocapture`
- `cargo test -p roz-cli --lib commands::session`
</verification>

<success_criteria>
- Copper segment archival produces one artifact row per segment and correct retention behavior.
- Bundle export includes MCAP + artifacts from live rows and verifies every digest.
</success_criteria>

<output>
After completion, create `.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-05-SUMMARY.md`.
</output>
