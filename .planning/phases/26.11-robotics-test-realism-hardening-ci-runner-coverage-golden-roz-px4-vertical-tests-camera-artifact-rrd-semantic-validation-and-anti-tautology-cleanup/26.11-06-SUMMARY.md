---
phase: "26.11"
plan: "06"
subsystem: testing
tags: [mavlink, px4, readiness, fixtures, replay, nightly]

requires:
  - phase: "26.11-01"
    provides: "nextest/nightly routing for px4_readiness_replay and px4_fixture_record_verify"
provides:
  - "Exact full-state PX4 readiness replay fixtures for TAKEOFF and LAND"
  - "Deterministic ReadinessBuilder test seam for exact heartbeat age assertions"
  - "Ignored, env-gated PX4 fixture recorder hygiene contract"
affects: [phase-27, mavlink-readiness, px4-fixtures, nightly-ci]

tech-stack:
  added: []
  patterns:
    - "Test-helper-only timestamp seams around Instant::now production logic"
    - "Verify-only fixture recording through tempdir output and checked-in fixture comparison"

key-files:
  created:
    - crates/roz-mavlink/tests/px4_readiness_replay.rs
    - crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json
    - crates/roz-mavlink/tests/fixtures/readiness/px4/land.json
    - crates/roz-mavlink/tests/px4_fixture_record_verify.rs
  modified:
    - crates/roz-mavlink/src/readiness.rs

key-decisions:
  - "Production readiness behavior remains unchanged; deterministic timestamps are exposed only under test/test-helpers."
  - "PX4 fixture recording is verify-only and writes to tempdir output; checked-in fixture mutation remains review-gated."
  - "ArduPilot readiness fixture parity remains deferred until an ArduPilot SITL container exists."
  - "STATE.md and ROADMAP.md were not updated or committed because the execution request explicitly excluded existing orchestrator dirty changes."

patterns-established:
  - "Full ReadinessState equality is the acceptance assertion for PX4 TAKEOFF/LAND checkpoints."
  - "Env-gated fixture recorders compare tempdir output to reviewed baselines and guard fixture git status."

requirements-completed: [TR-06]

duration: "15min"
completed: "2026-04-26"
---

# Phase 26.11 Plan 06: MAVLink PX4 Readiness Fixture Hardening Summary

**PX4 TAKEOFF/LAND readiness fixtures now replay into exact full-state `ReadinessState` equality, with fixture recording constrained to verify-only tempdir comparison.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-26T17:17:12Z
- **Completed:** 2026-04-26T17:32:13Z
- **Tasks:** 2
- **Files modified:** 5 implementation files plus this summary

## Accomplishments

- Added reviewed PX4 TAKEOFF and LAND readiness JSON fixtures with every `ReadinessState` field.
- Added exact full-state replay tests for both checkpoints through real MAVLink message structs.
- Added test-helper-only timestamp seams so `heartbeat_age_ms` can be asserted exactly.
- Added an ignored `ROZ_PX4_FIXTURE_RECORD_VERIFY` contract test that skips cleanly by default, records only to tempdir output when enabled, compares against checked-in fixtures, and guards fixture git status.

## Task Commits

1. **Task 1 RED:** `a3d8041` - `test(26.11-06): add failing PX4 readiness replay fixtures`
2. **Task 1 GREEN:** `386b3b3` - `feat(26.11-06): add deterministic readiness replay seam`
3. **Task 2 RED:** `fb56bfe` - `test(26.11-06): add failing PX4 fixture recorder contract`
4. **Task 2 GREEN:** `3821a67` - `feat(26.11-06): implement verify-only PX4 fixture recorder contract`
5. **Formatting:** `04124bc`, `0154214` - Plan 06 rustfmt alignment

## Files Created/Modified

- `crates/roz-mavlink/src/readiness.rs` - Added test-helper timestamp seam while preserving production `Instant::now()` behavior.
- `crates/roz-mavlink/tests/px4_readiness_replay.rs` - Replays TAKEOFF/LAND fixtures into `ReadinessBuilder` and asserts full `ReadinessState` equality.
- `crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json` - Armed PX4 TAKEOFF readiness baseline.
- `crates/roz-mavlink/tests/fixtures/readiness/px4/land.json` - Disarmed PX4 LAND readiness baseline.
- `crates/roz-mavlink/tests/px4_fixture_record_verify.rs` - Env-gated verify-only fixture recording hygiene contract.

## Decisions Made

- Kept deterministic time control behind `#[cfg(any(test, feature = "test-helpers"))]`.
- Used a test-local tempdir helper to stay inside the requested write scope instead of adding a new `roz-mavlink` dev-dependency.
- Preserved Phase 27 as the owner of the real PX4 SITL recorder; this plan only enforces fixture hygiene and reviewed-baseline comparison.

## Verification

- `cargo fmt --check` - **FAILED**, but only on unrelated dirty workstream files outside Plan 06 scope. Plan 06 files were checked separately with `cargo fmt --package roz-mavlink --check`, which passed.
- `cargo test -p roz-mavlink --features test-helpers --test px4_readiness_replay -- --nocapture` - **PASSED**: 2 passed, 0 failed.
- `cargo test -p roz-mavlink --features test-helpers --test px4_fixture_record_verify -- --ignored --nocapture` - **PASSED**: 1 passed, 0 failed; printed the required skip reason with `ROZ_PX4_FIXTURE_RECORD_VERIFY` unset.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Avoided out-of-scope dev-dependency changes**
- **Found during:** Task 2
- **Issue:** The plan expected `tempfile::tempdir()` behavior, but `crates/roz-mavlink/Cargo.toml` was outside the user-provided write scope.
- **Fix:** Added a small test-local `tempfile::tempdir()` RAII helper inside `px4_fixture_record_verify.rs`, preserving tempdir-only recorder behavior without touching Cargo manifests.
- **Files modified:** `crates/roz-mavlink/tests/px4_fixture_record_verify.rs`
- **Verification:** `cargo test -p roz-mavlink --features test-helpers --test px4_fixture_record_verify -- --ignored --nocapture`
- **Committed in:** `3821a67`

---

**Total deviations:** 1 auto-fixed (Rule 3)
**Impact on plan:** Behavior and verification intent are preserved; no checked-in fixture auto-mutation path was introduced.

## Issues Encountered

- Full-workspace `cargo fmt --check` is red because unrelated workstream files are currently unformatted or dirty. Plan 06 files are formatted and pass `cargo fmt --package roz-mavlink --check`.
- Several unrelated files remain dirty in the shared worktree. They were not staged or committed.

## Known Stubs

- `crates/roz-mavlink/tests/px4_fixture_record_verify.rs` uses `FakePx4FixtureRecorder` by design. The real PX4 SITL recorder remains Phase 27 scope; this plan verifies hygiene, tempdir output, checked-in baseline comparison, and git-status immutability.

## User Setup Required

None for default verification. To exercise the enabled recorder path manually, set `ROZ_PX4_FIXTURE_RECORD_VERIFY=1`; real SITL recording remains deferred to Phase 27.

## Next Phase Readiness

- Phase 27 can replace the fake recorder with a real PX4 SITL recorder while keeping the same verify-only tempdir and drift-check contract.
- ArduPilot fixture parity remains intentionally deferred.

## Self-Check: PASSED

- Created files exist on disk.
- Commits `a3d8041`, `386b3b3`, `fb56bfe`, `3821a67`, `04124bc`, and `0154214` exist in git history.
- No Plan 06 implementation files are left unstaged or dirty.

---
*Phase: 26.11*
*Completed: 2026-04-26*
