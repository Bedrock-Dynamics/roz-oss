---
phase: "26.11"
plan: "06"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/roz-mavlink/src/readiness.rs
  - crates/roz-mavlink/tests/px4_readiness_replay.rs
  - crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json
  - crates/roz-mavlink/tests/fixtures/readiness/px4/land.json
  - crates/roz-mavlink/tests/px4_fixture_record_verify.rs
autonomous: true
requirements: [TR-06]
tags: [mavlink, px4, readiness, fixtures, replay, nightly]

must_haves:
  truths:
    - "Readiness tests assert exact equality on the full `ReadinessState` at TAKEOFF and LAND checkpoints."
    - "PX4 fixture generation is verify-only: tempdir recording/comparison is allowed, checked-in fixture mutation is not automatic."
    - "ArduPilot fixture parity remains deferred until an ArduPilot SITL container exists."
    - "New ignored PX4/SITL-style tests are routed by nextest/nightly from Plan 01 and env-gated; they are not PR default."
  artifacts:
    - path: "crates/roz-mavlink/tests/px4_readiness_replay.rs"
      provides: "exact full-state PX4 readiness replay"
      contains: "px4_takeoff_readiness_matches_expected_full_state"
    - path: "crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json"
      provides: "reviewed TAKEOFF readiness fixture"
      contains: "fully_operational"
    - path: "crates/roz-mavlink/tests/px4_fixture_record_verify.rs"
      provides: "env-gated verify-only recorder contract"
      contains: "ROZ_PX4_FIXTURE_RECORD_VERIFY"
  key_links:
    - from: "crates/roz-mavlink/tests/px4_readiness_replay.rs"
      to: "crates/roz-mavlink/src/readiness.rs"
      via: "ReadinessBuilder apply_* then exact snapshot"
      pattern: "ReadinessBuilder"
    - from: "crates/roz-mavlink/tests/px4_fixture_record_verify.rs"
      to: "crates/roz-mavlink/tests/fixtures/readiness/px4"
      via: "tempdir compare without mutation"
      pattern: "tempdir"
---

<objective>
Harden MAVLink/PX4 readiness fixture evidence without implementing Phase 27 SITL: exact full-state replay now, verify-only fixture-record contract for nightly/env-gated runs.

Purpose: Phase 25/27 readiness logic must be protected by tests that assert all fields, while fixture generation must not silently rewrite baselines.
Output: Exact PX4 readiness replay fixtures and an env-gated verify-only recording harness.
</objective>

<execution_context>
@/Users/krnzt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/krnzt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
@.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-RESEARCH.md
@.planning/phases/27-nightly-px4-sitl-integration-ci-with-induced-nats-outage-liv/27-CONTEXT.md
@crates/roz-mavlink/src/readiness.rs
@crates/roz-mavlink/src/flight_command.rs
@crates/roz-mavlink/tests/qgc_coexistence.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact full-state PX4 readiness replay fixtures</name>
  <files>crates/roz-mavlink/src/readiness.rs, crates/roz-mavlink/tests/px4_readiness_replay.rs, crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json, crates/roz-mavlink/tests/fixtures/readiness/px4/land.json</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - .planning/phases/27-nightly-px4-sitl-integration-ci-with-induced-nats-outage-liv/27-CONTEXT.md
    - crates/roz-mavlink/src/readiness.rs
    - crates/roz-mavlink/tests/qgc_coexistence.rs
    - crates/roz-mavlink/Cargo.toml
  </read_first>
  <behavior>
    - Test `px4_takeoff_readiness_matches_expected_full_state`: replay TAKEOFF fixture messages and assert complete `ReadinessState` equality.
    - Test `px4_land_readiness_matches_expected_full_state`: replay LAND fixture messages and assert complete `ReadinessState` equality.
  </behavior>
  <action>
    Add a deterministic test seam to `crates/roz-mavlink/src/readiness.rs` only if needed to make `heartbeat_age_ms` exact. Preferred shape:

    ```rust
    #[cfg(any(test, feature = "test-helpers"))]
    impl ReadinessBuilder {
        pub fn apply_heartbeat_at_for_tests(&mut self, msg: &HEARTBEAT_DATA, rx_at: Instant) { ... }
        pub fn snapshot_at_for_tests(&self, now: Instant) -> ReadinessState { ... }
    }
    ```

    Keep production `apply_heartbeat` and `snapshot` behavior unchanged.

    Create `crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json` and `land.json`. Use JSON fixtures containing:
    - a `heartbeat` object with PX4 autopilot, armed status, and system_status;
    - a `gps_raw_int` object with `fix_type`;
    - an `estimator_status` object with `flags`;
    - an `expected` object containing every `ReadinessState` field: `heartbeat_alive`, `heartbeat_age_ms`, `armed`, `system_status`, `gps_fix_type`, `has_gps_fix`, `ekf_flags`, `ekf_converged`, `ready_to_arm`, `fully_operational`, `autopilot`.

    Create `crates/roz-mavlink/tests/px4_readiness_replay.rs` that parses those JSON fixtures, builds real `mavlink::common::{HEARTBEAT_DATA, GPS_RAW_INT_DATA, ESTIMATOR_STATUS_DATA}` structs, applies them to `ReadinessBuilder`, then asserts `assert_eq!(actual, expected)` on the complete proto `ReadinessState`.

    TAKEOFF expected posture:
    - `heartbeat_alive = true`
    - `armed = true`
    - `gps_fix_type = 3`
    - `has_gps_fix = true`
    - `ekf_converged = true`
    - `ready_to_arm = false`
    - `fully_operational = true`
    - `autopilot = ProtoMavAutopilot::Px4 as i32`

    LAND expected posture:
    - `heartbeat_alive = true`
    - `armed = false`
    - `gps_fix_type = 3`
    - `has_gps_fix = true`
    - `ekf_converged = true`
    - `ready_to_arm = true`
    - `fully_operational = true`
    - `autopilot = ProtoMavAutopilot::Px4 as i32`

    Do not add ArduPilot fixtures in this phase.
  </action>
  <verify>
    <automated>cargo test -p roz-mavlink --features test-helpers --test px4_readiness_replay -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "px4_takeoff_readiness_matches_expected_full_state" crates/roz-mavlink/tests/px4_readiness_replay.rs` returns one line.
    - `grep -F "px4_land_readiness_matches_expected_full_state" crates/roz-mavlink/tests/px4_readiness_replay.rs` returns one line.
    - `grep -F "assert_eq!(actual, expected" crates/roz-mavlink/tests/px4_readiness_replay.rs` returns at least one line.
    - `grep -F '"fully_operational": true' crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json` returns one line.
    - `grep -F '"ready_to_arm": true' crates/roz-mavlink/tests/fixtures/readiness/px4/land.json` returns one line.
    - `grep -R "ardupilot" crates/roz-mavlink/tests/fixtures/readiness/px4 crates/roz-mavlink/tests/px4_readiness_replay.rs` returns no lines.
    - `cargo test -p roz-mavlink --features test-helpers --test px4_readiness_replay -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>PX4 readiness replay asserts the full readiness state exactly for TAKEOFF and LAND.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add env-gated verify-only PX4 fixture recorder contract</name>
  <files>crates/roz-mavlink/tests/px4_fixture_record_verify.rs</files>
  <read_first>
    - .planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-CONTEXT.md
    - .planning/phases/27-nightly-px4-sitl-integration-ci-with-induced-nats-outage-liv/27-CONTEXT.md
    - crates/roz-mavlink/tests/px4_readiness_replay.rs
    - crates/roz-mavlink/tests/fixtures/readiness/px4/takeoff.json
    - .config/nextest.toml
    - .github/workflows/nightly.yml
  </read_first>
  <behavior>
    - Ignored test `px4_fixture_record_verify_writes_only_tempdir_and_compares_checked_in`: when `ROZ_PX4_FIXTURE_RECORD_VERIFY=1` is absent, prints a skip reason and returns; when present, writes recorded fixture output to a tempdir and compares to checked-in fixtures without modifying repo files.
  </behavior>
  <action>
    Create `crates/roz-mavlink/tests/px4_fixture_record_verify.rs`.

    This is not the full Phase 27 SITL scenario. It is a contract test for fixture hygiene:
    - Mark it `#[ignore = "env-gated PX4 fixture recording verify-only; set ROZ_PX4_FIXTURE_RECORD_VERIFY=1"]`.
    - If `ROZ_PX4_FIXTURE_RECORD_VERIFY` is not exactly `1`, return early after printing `ROZ_PX4_FIXTURE_RECORD_VERIFY not set; skipping verify-only PX4 fixture recording`.
    - When enabled, write any newly recorded readiness/compliance artifacts under `tempfile::tempdir()`, never under `crates/roz-mavlink/tests/fixtures`.
    - Compare tempdir output to checked-in fixture files. If different, fail with a message containing `fixture drift requires a reviewed PR`.
    - Include a guard that snapshots `git status --short -- crates/roz-mavlink/tests/fixtures` before and after the test and asserts it is unchanged.

    If there is no local PX4 SITL recorder available yet, implement the recorder function as a small internal trait with a fake implementation used by the test; leave a concrete `record_px4_fixtures_to_dir(output_dir: &Path)` function returning `anyhow::bail!("PX4 SITL recorder lands in Phase 27")` unless the env var is set. The hygiene assertions must still compile and run. Do not broaden this task into a PX4 SITL implementation.
  </action>
  <verify>
    <automated>cargo test -p roz-mavlink --features test-helpers --test px4_fixture_record_verify -- --ignored --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "ROZ_PX4_FIXTURE_RECORD_VERIFY" crates/roz-mavlink/tests/px4_fixture_record_verify.rs` returns at least two lines.
    - `grep -F "tempfile::tempdir" crates/roz-mavlink/tests/px4_fixture_record_verify.rs` returns at least one line.
    - `grep -F "fixture drift requires a reviewed PR" crates/roz-mavlink/tests/px4_fixture_record_verify.rs` returns at least one line.
    - `grep -E "write\\(|File::create|fs::write" crates/roz-mavlink/tests/px4_fixture_record_verify.rs` returns no lines that target `crates/roz-mavlink/tests/fixtures`.
    - `grep -F "px4_fixture_record_verify" .config/nextest.toml` returns one line from Plan 01 routing.
    - `cargo test -p roz-mavlink --features test-helpers --test px4_fixture_record_verify -- --ignored --nocapture` exits 0 without `ROZ_PX4_FIXTURE_RECORD_VERIFY=1`.
  </acceptance_criteria>
  <done>Fixture generation is documented and tested as verify-only, tempdir-based, and review-gated.</done>
</task>

</tasks>

<verification>
- `cargo fmt --check`
- `cargo test -p roz-mavlink --features test-helpers --test px4_readiness_replay -- --nocapture`
- `cargo test -p roz-mavlink --features test-helpers --test px4_fixture_record_verify -- --ignored --nocapture`
</verification>

<success_criteria>
- PX4 TAKEOFF/LAND readiness replay asserts complete `ReadinessState` values.
- Fixture recorder contract cannot auto-mutate checked-in fixtures.
- ArduPilot remains deferred, and SITL remains Phase 27/nightly scope.
</success_criteria>

<output>
After completion, create `.planning/phases/26.11-robotics-test-realism-hardening-ci-runner-coverage-manipulator-px4-vertical-tests-camera-artifact-rrd-semantic-validation-and-anti-tautology-cleanup/26.11-06-SUMMARY.md`.
</output>
